From 114678e8bc3ff21db48e9ee496ae829643cbf9f4 Mon Sep 17 00:00:00 2001
From: Hiren Gohel <vhiren@qti.qualcomm.com>
Date: Mon, 10 Aug 2026 13:51:04 +0530
Subject: [PATCH] dwc3: gadget: Fix compliance TD 9.23 compliance issue

On repeated HS PIPO cycling, USB compliance flags a yellow-bang
condition. WA is to run the eUSB2 PHY calibrate sequence on every
CONNECT_DONE. phy_calibrate() takes phy->mutex, which cannot be
called from dwc3_gadget_conndone_interrupt() as it runs under
dwc->lock (spinlock) inside the threaded IRQ handler.

Defer only the call, not the timing: set a flag in the conndone
handler under the lock, then run phy_calibrate() in
dwc3_thread_interrupt() after local_bh_enable(), once fully back in
preemptible thread context. This keeps the calibrate synchronous
with the CONNECT_DONE event (no workqueue latency) while avoiding
sleeping-in-atomic-context.

Change-Id: Ib4484b1902dc8d2f8c4b38cf4cebc72c12b4f7bd
Signed-off-by: Hiren Gohel <vhiren@qti.qualcomm.com>
---
 drivers/usb/dwc3/core.h   | 1 +
 drivers/usb/dwc3/gadget.c | 9 +++++++++
 2 files changed, 10 insertions(+)

diff --git a/drivers/usb/dwc3/core.h b/drivers/usb/dwc3/core.h
index 564b41ba8648..1801e6a8f110 100644
--- a/drivers/usb/dwc3/core.h
+++ b/drivers/usb/dwc3/core.h
@@ -1414,6 +1414,7 @@ struct dwc3 {
 	bool			cable_disconnected;
 	bool			dwc3_guctl_resbwhseps_quirk;
 	u32			eusb2_phy_bitmap;
+	bool                    eusb2_needs_calibrate;
 };
 
 #define INCRX_BURST_MODE 0
diff --git a/drivers/usb/dwc3/gadget.c b/drivers/usb/dwc3/gadget.c
index 4c3879ce1683..499004ee497e 100644
--- a/drivers/usb/dwc3/gadget.c
+++ b/drivers/usb/dwc3/gadget.c
@@ -4187,6 +4187,9 @@ static void dwc3_gadget_conndone_interrupt(struct dwc3 *dwc)
 	speed = reg & DWC3_DSTS_CONNECTSPD;
 	dwc->speed = speed;
 
+	if (dwc->eusb2_phy_bitmap & BIT(0) && (speed >= DWC3_DSTS_SUPERSPEED))
+		dwc->eusb2_needs_calibrate = true;
+
 	if (DWC3_IP_IS(DWC32))
 		lanes = DWC3_DSTS_CONNLANES(reg) + 1;
 
@@ -4572,6 +4575,12 @@ static irqreturn_t dwc3_thread_interrupt(int irq, void *_evt)
 	spin_unlock_irqrestore(&dwc->lock, flags);
 	local_bh_enable();
 
+	if (dwc->eusb2_needs_calibrate) {
+		dwc->eusb2_needs_calibrate = false;
+		phy_calibrate(dwc->usb2_generic_phy);
+	}
+
+
 	return ret;
 }
 
-- 
2.34.1

