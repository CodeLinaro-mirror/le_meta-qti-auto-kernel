From dec5d53b20287abdd5112f6c88326a2ba4b0ad18 Mon Sep 17 00:00:00 2001
From: Aniket Randive <arandive@qti.qualcomm.com>
Date: Thu, 10 Jul 2025 16:41:31 +0530
Subject: [PATCH] meta-qti-auto-kernel: ccu: Add Ftrace support for CCU

Add Ftrace support for CCU, GPI and I2C driver to get the debug logs.

Change-Id: Ibdf06f61f227533c47c949bfbd8088a2d6d706ab
Signed-off-by: Aniket Randive <arandive@qti.qualcomm.com>
---
 drivers/dma/qcom/gpi.c                  | 394 +++++++++++++++---------
 drivers/i2c/busses/i2c-qcom-geni.c      | 337 ++++++++++++--------
 drivers/soc/qcom/qcom_ccu_qup.c         | 172 ++++++++---
 drivers/spi/spi-geni-qcom.c             |   4 +-
 drivers/tty/serial/qcom_geni_serial.c   |   4 +-
 include/trace/events/ccu_qup_trace.h    |  15 +
 include/trace/events/gpi_qup_trace.h    |  16 +
 include/trace/events/qup_buses_trace.h  | 113 -------
 include/trace/events/qup_i2c_trace.h    |  15 +
 include/trace/events/qup_serial_trace.h |  31 ++
 include/trace/events/qup_spi_trace.h    |  21 ++
 include/trace/events/qup_trace_common.h |  65 ++++
 12 files changed, 761 insertions(+), 426 deletions(-)
 create mode 100644 include/trace/events/ccu_qup_trace.h
 create mode 100644 include/trace/events/gpi_qup_trace.h
 delete mode 100644 include/trace/events/qup_buses_trace.h
 create mode 100644 include/trace/events/qup_i2c_trace.h
 create mode 100644 include/trace/events/qup_serial_trace.h
 create mode 100644 include/trace/events/qup_spi_trace.h
 create mode 100644 include/trace/events/qup_trace_common.h

diff --git a/drivers/dma/qcom/gpi.c b/drivers/dma/qcom/gpi.c
index 9ca3f8787da0..dab1ed5aee4c 100644
--- a/drivers/dma/qcom/gpi.c
+++ b/drivers/dma/qcom/gpi.c
@@ -13,12 +13,48 @@
 #include <linux/of_dma.h>
 #include <linux/platform_device.h>
 #include <linux/dma/qcom-gpi-dma.h>
+#include <linux/kernel.h>
+#include <linux/device.h>
+#include <linux/sysfs.h>
 #include <linux/scatterlist.h>
 #include <linux/slab.h>
 #include <linux/soc/qcom/qcom_ccu_qup.h>
 #include "../dmaengine.h"
 #include "../virt-dma.h"
 
+#define CREATE_TRACE_POINTS
+#include <trace/events/gpi_qup_trace.h>
+
+enum DEBUG_LOG_LVL {
+	LOG_LVL_ERROR,
+	LOG_LVL_INFO,
+	LOG_LVL_VERBOSE,
+	LOG_LVL_MAX,
+};
+
+#define GPI_LOG(log_fn, level, dev, fmt, ...) do { \
+	if ((level) <= log_level) \
+		gpi_trace_log(dev, fmt, ##__VA_ARGS__); \
+	else \
+		log_fn(dev, fmt, ##__VA_ARGS__); \
+} while (0)
+
+#define GPI_ERR_LOG(level, dev, fmt, ...) GPI_LOG(dev_err, level, dev, fmt, ##__VA_ARGS__)
+#define GPI_DBG_LOG(level, dev, fmt, ...) GPI_LOG(dev_dbg, level, dev, fmt, ##__VA_ARGS__)
+
+void gpi_trace_log(struct device *dev, const char *fmt, ...)
+{
+	va_list args;
+	struct va_format vaf;
+
+	va_start(args, fmt);
+	vaf.fmt = fmt;
+	vaf.va = &args;
+
+	trace_gpi_log_info(dev_name(dev), &vaf);
+	va_end(args);
+}
+
 #define TRE_TYPE_DMA		0x10
 #define TRE_TYPE_GO		0x20
 #define TRE_TYPE_CONFIG0	0x22
@@ -708,6 +744,27 @@ static void gpi_process_events(struct gpii *gpii);
 static int gpi_send_cmd(struct gpii *gpii, struct gchan *gchan,
 			enum gpi_cmd gpi_cmd);
 
+static int log_level = -1;
+static ssize_t log_level_show(struct device *dev,
+			      struct device_attribute *attr, char *buf)
+{
+	return snprintf(buf, PAGE_SIZE, "%d\n", log_level);
+}
+
+static ssize_t log_level_store(struct device *dev,
+			       struct device_attribute *attr,
+			       const char *buf, size_t count)
+{
+	int val;
+
+	if (kstrtoint(buf, 10, &val) != 0 || val < LOG_LVL_ERROR || val >= LOG_LVL_MAX)
+		return -EINVAL;
+
+	log_level = val;
+	return count;
+}
+static DEVICE_ATTR_RW(log_level);
+
 static inline struct gchan *to_gchan(struct dma_chan *dma_chan)
 {
 	return container_of(dma_chan, struct gchan, vc.chan);
@@ -767,16 +824,19 @@ int ccu_gpi_prioritize_chan(struct dma_chan *chan)
 	int ret = 0;
 
 	/* Check Previous Transfer completed or not */
-	dev_dbg(gpii->gpi_dev->dev, "wp:0x%0llx rp:0x%0llx\n", ring->wp, ring->rp);
 	if (ring->wp == ring->rp) {
 		/* send command to Prioritize the channel */
 		ret = gpi_send_cmd(gpii, gchan, GPI_CH_CMD_PRIORITIZE_REG_CHAN);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error to prioritize reg Chan:%d\n", ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "Error to prioritize reg Chan:%d\n", ret);
 			return -EIO;
 		}
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "%s: Prioritized the chan:%d of GPII:%d\n",
+			    __func__, gchan->chid, gpii->gpii_id);
 	} else {
-		dev_err(gpii->gpi_dev->dev, "Pending TRE's are present\n");
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "Pending TRE's are present\n");
 		return -EAGAIN;
 	}
 
@@ -808,6 +868,8 @@ void ccu_gpi_configure_mode(struct dma_chan *chan, int mode, int type)
 					   gpii->gpi_dev->cfg.irq_off),
 					   GPII_n_CNTXT_TYPE_COMP_IRQ_MSK_BMSK, enable);
 	}
+	GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+		    "%s: mode:%d, type:%d\n", __func__, mode, type);
 }
 EXPORT_SYMBOL(ccu_gpi_configure_mode);
 
@@ -826,6 +888,7 @@ void ccu_gpi_set_premature_cancel_flag(struct dma_chan *chan)
 	struct gpii *gpii = gchan->gpii;
 
 	gpii->premature_cancel = true;
+	GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev, "%s: Premature cancel flag set\n", __func__);
 }
 EXPORT_SYMBOL(ccu_gpi_set_premature_cancel_flag);
 
@@ -886,8 +949,8 @@ static int gpi_config_interrupts(struct gpii *gpii, enum gpii_irq_settings setti
 				       gpi_handle_irq, IRQF_TRIGGER_HIGH,
 				       "gpi-dma", gpii);
 		if (ret < 0) {
-			dev_err(gpii->gpi_dev->dev, "error request irq:%d ret:%d\n",
-				gpii->irq, ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "error request irq:%d ret:%d\n", gpii->irq, ret);
 			return ret;
 		}
 	}
@@ -969,8 +1032,8 @@ static int gpi_send_cmd(struct gpii *gpii, struct gchan *gchan,
 	if (IS_CHAN_CMD(gpi_cmd))
 		chid = gchan->chid;
 
-	dev_dbg(gpii->gpi_dev->dev,
-		"sending cmd: %s:%u\n", TO_GPI_CMD_STR(gpi_cmd), chid);
+	GPI_DBG_LOG(LOG_LVL_VERBOSE, gpii->gpi_dev->dev,
+		    "sending cmd: %s:%u\n", TO_GPI_CMD_STR(gpi_cmd), chid);
 
 	/* send opcode and wait for completion */
 	reinit_completion(&gpii->cmd_completion);
@@ -983,8 +1046,8 @@ static int gpi_send_cmd(struct gpii *gpii, struct gchan *gchan,
 	timeout = wait_for_completion_timeout(&gpii->cmd_completion,
 					      msecs_to_jiffies(CMD_TIMEOUT_MS));
 	if (!timeout) {
-		dev_err(gpii->gpi_dev->dev, "cmd: %s completion timeout:%u\n",
-			TO_GPI_CMD_STR(gpi_cmd), chid);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "cmd: %s completion timeout:%u\n",
+			    TO_GPI_CMD_STR(gpi_cmd), chid);
 		return -EIO;
 	}
 
@@ -1082,7 +1145,7 @@ static void gpi_process_gen_err_irq(struct gpii *gpii)
 	u32 irq_stts = gpi_read_reg(gpii, gpii->regs + offset);
 
 	/* clear the status */
-	dev_dbg(gpii->gpi_dev->dev, "irq_stts:0x%x\n", irq_stts);
+	GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev, "irq_stts:0x%x\n", irq_stts);
 
 	/* Clear the register */
 	offset = GPII_n_CNTXT_GPII_IRQ_CLR_OFFS(gpii_id, gpii->gpi_dev->cfg.irq_off);
@@ -1103,13 +1166,14 @@ static void gpi_process_glob_err_irq(struct gpii *gpii)
 
 	/* only error interrupt should be set */
 	if (irq_stts & ~GPI_GLOB_IRQ_ERROR_INT_MSK) {
-		dev_err(gpii->gpi_dev->dev, "invalid error status:0x%x\n", irq_stts);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "invalid error status:0x%x\n", irq_stts);
 		return;
 	}
 
 	offset = GPII_n_ERROR_LOG_OFFS(gpii_id, gpii->gpi_dev->cfg.irq_off);
 	status = gpi_read_reg(gpii, gpii->regs + offset);
-	dev_err(gpii->gpi_dev->dev, "GLOBAL Error LOG Reg:0x%x\n", status);
+	GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev, "GLOBAL Error LOG Reg:0x%x\n", status);
 	gpi_write_reg(gpii, gpii->regs + offset, 0);
 }
 
@@ -1127,8 +1191,9 @@ static void process_gpi_irq(struct gpii *gpii)
 	 * while software state is in DISABLE state, bailing out.
 	 */
 	if (!REG_ACCESS_VALID(gpii->pm_state)) {
-		dev_err(gpii->gpi_dev->dev, "receive interrupt while in %s state\n",
-			TO_GPI_PM_STR(gpii->pm_state));
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "receive interrupt while in %s state\n",
+			    TO_GPI_PM_STR(gpii->pm_state));
 		goto exit_irq;
 	}
 
@@ -1144,6 +1209,8 @@ static void process_gpi_irq(struct gpii *gpii)
 	do {
 		/* global gpii error */
 		if (type & GPII_n_CNTXT_TYPE_IRQ_MSK_GLOB) {
+			GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+				    "processing Global interrupt for GPII:%d\n", gpii_id);
 			gpi_process_glob_err_irq(gpii);
 			type &= ~(GPII_n_CNTXT_TYPE_IRQ_MSK_GLOB);
 		}
@@ -1151,6 +1218,8 @@ static void process_gpi_irq(struct gpii *gpii)
 		/* transfer complete interrupt */
 		if ((type & GPII_n_CNTXT_TYPE_IRQ_MSK_IEOB) ||
 			(type & GPII_n_CNTXT_TYPE_COMP_IRQ_MSK_IEOB)) {
+			GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+				    "processing IEOB interrupt for GPII:%d\n", gpii_id);
 			gpi_process_ieob(gpii);
 			type &= ~GPII_n_CNTXT_TYPE_IRQ_MSK_IEOB;
 		}
@@ -1160,8 +1229,8 @@ static void process_gpi_irq(struct gpii *gpii)
 			u32 ev_state;
 			u32 ev_ch_irq;
 
-			dev_dbg(gpii->gpi_dev->dev,
-				"processing EV CTRL interrupt\n");
+			GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+				    "processing EV CTRL interrupt for GPII:%d\n", gpii_id);
 			offset = GPII_n_CNTXT_SRC_EV_CH_IRQ_OFFS
 				(gpii_id, gpii->gpi_dev->cfg.irq_off);
 			ev_ch_irq = gpi_read_reg(gpii, gpii->regs + offset);
@@ -1182,21 +1251,24 @@ static void process_gpi_irq(struct gpii *gpii)
 				ev_state = DEFAULT_EV_CH_STATE;
 
 			gpii->ev_state = ev_state;
-			dev_dbg(gpii->gpi_dev->dev, "setting EV state to %s\n",
-				TO_GPI_EV_STATE_STR(gpii->ev_state));
+			GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+				    "setting EV state to %s of GPII:%d\n",
+				    TO_GPI_EV_STATE_STR(gpii->ev_state), gpii_id);
 			complete_all(&gpii->cmd_completion);
 			type &= ~(GPII_n_CNTXT_TYPE_IRQ_MSK_EV_CTRL);
 		}
 
 		/* channel control irq */
 		if (type & GPII_n_CNTXT_TYPE_IRQ_MSK_CH_CTRL) {
-			dev_dbg(gpii->gpi_dev->dev, "process CH CTRL interrupts\n");
+			GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+				    "process CH CTRL interrupts of GPII:%d\n", gpii_id);
 			gpi_process_ch_ctrl_irq(gpii);
 			type &= ~(GPII_n_CNTXT_TYPE_IRQ_MSK_CH_CTRL);
 		}
 
 		if (type) {
-			dev_err(gpii->gpi_dev->dev, "Unhandled interrupt status:0x%x\n", type);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "Unhandled interrupt status:0x%x of GPII:%d\n", type, gpii_id);
 			gpi_process_gen_err_irq(gpii);
 			goto exit_irq;
 		}
@@ -1247,8 +1319,9 @@ static void gpi_process_ccu_error_event(struct gchan *gchan,
 	unsigned long flags;
 
 	if (gchan->pm_state != ACTIVE_STATE) {
-		dev_err(gpii->gpi_dev->dev, "skipping processing event because ch @ %s state\n",
-			TO_GPI_PM_STR(gchan->pm_state));
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "skipping processing event because ch @ %s state\n",
+			    TO_GPI_PM_STR(gchan->pm_state));
 		return;
 	}
 
@@ -1258,12 +1331,13 @@ static void gpi_process_ccu_error_event(struct gchan *gchan,
 		struct gpi_ere *gpi_ere;
 
 		spin_unlock_irqrestore(&gchan->vc.lock, flags);
-		dev_err(gpii->gpi_dev->dev, "Event without a pending descriptor!\n");
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "Event without a pending descriptor!\n");
 		gpi_ere = (struct gpi_ere *)error_event;
-		dev_err(gpii->gpi_dev->dev,
-			"Event: %08x %08x %08x %08x\n",
-			gpi_ere->dword[0], gpi_ere->dword[1],
-			gpi_ere->dword[2], gpi_ere->dword[3]);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "Event: %08x %08x %08x %08x\n",
+			    gpi_ere->dword[0], gpi_ere->dword[1],
+			    gpi_ere->dword[2], gpi_ere->dword[3]);
 		return;
 	}
 	gpi_desc = to_gpi_desc(vd);
@@ -1274,7 +1348,7 @@ static void gpi_process_ccu_error_event(struct gchan *gchan,
 
 	if (error_event->code == MSM_GPI_TCE_UNEXP_ERR &&
 			error_event->type == CCU_ERROR_NOTIFICATION_EV_TYPE) {
-		dev_err(gpii->gpi_dev->dev, "I2C CCU SYNC TRE Not Found\n");
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "I2C CCU SYNC TRE Not Found\n");
 		result.result = DMA_TRANS_ABORTED;
 		result.gpii_id = gpii->gpii_id;
 	}
@@ -1306,8 +1380,9 @@ static void gpi_process_imed_data_event(struct gchan *gchan,
 	 * If channel not active don't process event
 	 */
 	if (gchan->pm_state != ACTIVE_STATE) {
-		dev_err(gpii->gpi_dev->dev, "skipping processing event because ch @ %s state\n",
-			TO_GPI_PM_STR(gchan->pm_state));
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "skipping processing event because ch @ %s state\n",
+			    TO_GPI_PM_STR(gchan->pm_state));
 		return;
 	}
 
@@ -1318,17 +1393,19 @@ static void gpi_process_imed_data_event(struct gchan *gchan,
 		struct gpi_tre *gpi_tre;
 
 		spin_unlock_irqrestore(&gchan->vc.lock, flags);
-		dev_dbg(gpii->gpi_dev->dev, "event without a pending descriptor!\n");
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "event without a pending descriptor! for GPII:%d\n",
+			    gpii->gpii_id);
 		gpi_ere = (struct gpi_ere *)imed_event;
-		dev_dbg(gpii->gpi_dev->dev,
-			"Event: %08x %08x %08x %08x\n",
-			gpi_ere->dword[0], gpi_ere->dword[1],
-			gpi_ere->dword[2], gpi_ere->dword[3]);
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "GPII:%d Event: %08x %08x %08x %08x\n",
+			    gpii->gpii_id, gpi_ere->dword[0], gpi_ere->dword[1],
+			    gpi_ere->dword[2], gpi_ere->dword[3]);
 		gpi_tre = tre;
-		dev_dbg(gpii->gpi_dev->dev,
-			"Pending TRE: %08x %08x %08x %08x\n",
-			gpi_tre->dword[0], gpi_tre->dword[1],
-			gpi_tre->dword[2], gpi_tre->dword[3]);
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "GPII:%d Pending TRE: %08x %08x %08x %08x\n",
+			    gpii->gpii_id, gpi_tre->dword[0], gpi_tre->dword[1],
+			    gpi_tre->dword[2], gpi_tre->dword[3]);
 		return;
 	}
 	gpi_desc = to_gpi_desc(vd);
@@ -1391,8 +1468,9 @@ static void gpi_process_xfer_compl_event(struct gchan *gchan,
 	 * even when the channel is no longer active.
 	 */
 	if (!gpii->premature_cancel && unlikely(gchan->pm_state != ACTIVE_STATE)) {
-		dev_err(gpii->gpi_dev->dev, "skipping processing event because ch @ %s state\n",
-			TO_GPI_PM_STR(gchan->pm_state));
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "skipping processing event because ch @ %s state\n",
+			    TO_GPI_PM_STR(gchan->pm_state));
 		return;
 	}
 
@@ -1402,12 +1480,13 @@ static void gpi_process_xfer_compl_event(struct gchan *gchan,
 		struct gpi_ere *gpi_ere;
 
 		spin_unlock_irqrestore(&gchan->vc.lock, flags);
-		dev_err(gpii->gpi_dev->dev, "Event without a pending descriptor!\n");
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "Event without a pending descriptor!\n");
 		gpi_ere = (struct gpi_ere *)compl_event;
-		dev_err(gpii->gpi_dev->dev,
-			"Event: %08x %08x %08x %08x\n",
-			gpi_ere->dword[0], gpi_ere->dword[1],
-			gpi_ere->dword[2], gpi_ere->dword[3]);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "Event: %08x %08x %08x %08x\n",
+			    gpi_ere->dword[0], gpi_ere->dword[1],
+			    gpi_ere->dword[2], gpi_ere->dword[3]);
 		return;
 	}
 
@@ -1435,15 +1514,17 @@ static void gpi_process_xfer_compl_event(struct gchan *gchan,
 	}
 
 	if (compl_event->code == MSM_GPI_TCE_UNEXP_ERR) {
-		dev_err(gpii->gpi_dev->dev, "Error in Transaction\n");
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "Error in Transaction\n");
 		result.result = DMA_TRANS_ABORTED;
 	} else {
-		dev_dbg(gpii->gpi_dev->dev, "Transaction Success\n");
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "Transaction Success for GPII:%d\n", gpii->gpii_id);
 		result.result = DMA_TRANS_NOERROR;
 	}
 	result.residue = gpi_desc->len - compl_event->length;
 	result.gpii_id = gpii->gpii_id;
-	dev_dbg(gpii->gpi_dev->dev, "Residue %d\n", result.residue);
+	GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+		    "GPII:%d Residue %d\n", gpii->gpii_id, result.residue);
 
 	dma_cookie_complete(&vd->tx);
 	if (gchan->protocol == QCOM_GPI_I2C) {
@@ -1484,11 +1565,11 @@ static void gpi_process_events(struct gpii *gpii)
 			chid = gpi_event->xfer_compl_event.chid;
 			type = gpi_event->xfer_compl_event.type;
 
-			dev_dbg(gpii->gpi_dev->dev,
-				"Event: CHID:%u, type:%x %08x %08x %08x %08x\n",
-				chid, type, gpi_event->gpi_ere.dword[0],
-				gpi_event->gpi_ere.dword[1], gpi_event->gpi_ere.dword[2],
-				gpi_event->gpi_ere.dword[3]);
+			GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+				    "GPII:%d Event: CHID:%u, type:%x %08x %08x %08x %08x\n",
+				    gpii->gpii_id, chid, type, gpi_event->gpi_ere.dword[0],
+				    gpi_event->gpi_ere.dword[1], gpi_event->gpi_ere.dword[2],
+				    gpi_event->gpi_ere.dword[3]);
 
 			switch (type) {
 			case XFER_COMPLETE_EV_TYPE:
@@ -1497,7 +1578,8 @@ static void gpi_process_events(struct gpii *gpii)
 							     &gpi_event->xfer_compl_event);
 				break;
 			case STALE_EV_TYPE:
-				dev_dbg(gpii->gpi_dev->dev, "stale event, not processing\n");
+				GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+					    "GPII:%d stale event, not processing\n", gpii->gpii_id);
 				break;
 			case IMMEDIATE_DATA_EV_TYPE:
 				gchan = &gpii->gchan[chid];
@@ -1505,17 +1587,20 @@ static void gpi_process_events(struct gpii *gpii)
 							    &gpi_event->immediate_data_event);
 				break;
 			case QUP_NOTIF_EV_TYPE:
-				dev_dbg(gpii->gpi_dev->dev, "QUP_NOTIF_EV_TYPE\n");
+				GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+					    "GPII:%d QUP_NOTIF_EV_TYPE\n", gpii->gpii_id);
 				break;
 			case CCU_ERROR_NOTIFICATION_EV_TYPE:
-				dev_dbg(gpii->gpi_dev->dev, "CCU error notifcation event\n");
+				GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+					    "GPII:%d CCU error notifcation event\n", gpii->gpii_id);
 				gchan = &gpii->gchan[chid];
 				gpi_process_ccu_error_event(gchan,
 							    &gpi_event->ccu_error_notif_event);
 				break;
 			default:
-				dev_dbg(gpii->gpi_dev->dev,
-					"not supported event type:0x%x\n", type);
+				GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+					    "GPII:%d not supported event type:0x%x\n",
+					    gpii->gpii_id, type);
 			}
 			gpi_ring_recycle_ev_element(ev_ring);
 		}
@@ -1539,8 +1624,9 @@ static void gpi_ev_tasklet(unsigned long data)
 	read_lock(&gpii->pm_lock);
 	if (!REG_ACCESS_VALID(gpii->pm_state)) {
 		read_unlock(&gpii->pm_lock);
-		dev_err(gpii->gpi_dev->dev, "not processing any events, pm_state:%s\n",
-			TO_GPI_PM_STR(gpii->pm_state));
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "not processing any events, pm_state:%s\n",
+			    TO_GPI_PM_STR(gpii->pm_state));
 		return;
 	}
 
@@ -1589,8 +1675,8 @@ static int gpi_reset_chan(struct gchan *gchan, enum gpi_cmd gpi_cmd)
 
 	ret = gpi_send_cmd(gpii, gchan, gpi_cmd);
 	if (ret) {
-		dev_err(gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
-			TO_GPI_CMD_STR(gpi_cmd), ret);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
+			    TO_GPI_CMD_STR(gpi_cmd), ret);
 		return ret;
 	}
 
@@ -1622,8 +1708,8 @@ static int gpi_start_chan(struct gchan *gchan)
 
 	ret = gpi_send_cmd(gpii, gchan, GPI_CH_CMD_START);
 	if (ret) {
-		dev_err(gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
-			TO_GPI_CMD_STR(GPI_CH_CMD_START), ret);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
+			    TO_GPI_CMD_STR(GPI_CH_CMD_START), ret);
 		return ret;
 	}
 
@@ -1642,8 +1728,8 @@ static int gpi_stop_chan(struct gchan *gchan)
 
 	ret = gpi_send_cmd(gpii, gchan, GPI_CH_CMD_STOP);
 	if (ret) {
-		dev_err(gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
-			TO_GPI_CMD_STR(GPI_CH_CMD_STOP), ret);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
+			    TO_GPI_CMD_STR(GPI_CH_CMD_STOP), ret);
 		return ret;
 	}
 
@@ -1663,8 +1749,8 @@ static int gpi_alloc_chan(struct gchan *chan, bool send_alloc_cmd)
 	if (send_alloc_cmd) {
 		ret = gpi_send_cmd(gpii, chan, GPI_CH_CMD_ALLOCATE);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
-				TO_GPI_CMD_STR(GPI_CH_CMD_ALLOCATE), ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
+				    TO_GPI_CMD_STR(GPI_CH_CMD_ALLOCATE), ret);
 			return ret;
 		}
 	}
@@ -1715,8 +1801,8 @@ static int gpi_alloc_ev_chan(struct gpii *gpii)
 
 	ret = gpi_send_cmd(gpii, NULL, GPI_EV_CMD_ALLOCATE);
 	if (ret) {
-		dev_err(gpii->gpi_dev->dev, "error with cmd:%s ret:%d\n",
-			TO_GPI_CMD_STR(GPI_EV_CMD_ALLOCATE), ret);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "error with cmd:%s ret:%d\n",
+			    TO_GPI_CMD_STR(GPI_EV_CMD_ALLOCATE), ret);
 		return ret;
 	}
 
@@ -1818,17 +1904,17 @@ static int gpi_alloc_ring(struct gpi_ring *ring, u32 elements,
 		bit++;
 	len = 1 << bit;
 	ring->alloc_size = (len + (len - 1));
-	dev_dbg(gpii->gpi_dev->dev,
-		"#el:%u el_size:%u len:%u actual_len:%llu alloc_size:%zu\n",
-		  elements, el_size, (elements * el_size), len,
-		  ring->alloc_size);
+	GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+		    "GPII:%d #el:%u el_size:%u len:%u actual_len:%llu alloc_size:%zu\n",
+		    gpii->gpii_id, elements, el_size, (elements * el_size), len,
+		    ring->alloc_size);
 
 	ring->pre_aligned = dma_alloc_coherent(gpii->gpi_dev->dev,
 					       ring->alloc_size,
 					       &ring->dma_handle, GFP_KERNEL);
 	if (!ring->pre_aligned) {
-		dev_err(gpii->gpi_dev->dev, "could not alloc size:%zu mem for ring\n",
-			ring->alloc_size);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "could not alloc size:%zu mem for ring\n", ring->alloc_size);
 		return -ENOMEM;
 	}
 
@@ -1846,10 +1932,10 @@ static int gpi_alloc_ring(struct gpi_ring *ring, u32 elements,
 	/* update to other cores */
 	smp_wmb();
 
-	dev_dbg(gpii->gpi_dev->dev,
-		"phy_pre:%pad phy_alig:%pa len:%u el_size:%u elements:%u\n",
-		&ring->dma_handle, &ring->phys_addr, ring->len,
-		ring->el_size, ring->elements);
+	GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+		    "GPII:%d phy_pre:%pad phy_alig:%pa len:%u el_size:%u elements:%u\n",
+		    gpii->gpii_id, &ring->dma_handle, &ring->phys_addr, ring->len,
+		    ring->el_size, ring->elements);
 
 	return 0;
 }
@@ -1864,7 +1950,8 @@ static void gpi_queue_xfer(struct gpii *gpii, struct gchan *gchan,
 	/* get next tre location we can copy */
 	ret = gpi_ring_add_element(&gchan->ch_ring, (void **)&ch_tre);
 	if (unlikely(ret)) {
-		dev_err(gpii->gpi_dev->dev, "Error adding ring element to xfer ring\n");
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "Error adding ring element to xfer ring\n");
 		return;
 	}
 
@@ -1886,14 +1973,15 @@ static void gpi_nop_tre(struct gpii *gpii, struct gchan *gchan)
 	local_wp = to_physical(ring, ring->wp);
 	cntxt_rp = ring->rp;
 
-	dev_dbg(gpii->gpi_dev->dev, "chid:%d, rp:0x%llx, wp:0x%llx\n",
-		gchan->chid, local_rp, local_wp);
+	GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev, "GPII:%d chid:%d, rp:0x%llx, wp:0x%llx\n",
+		    gpii->gpii_id, gchan->chid, local_rp, local_wp);
 
 	while (local_rp != local_wp) {
 		tre = (struct gpi_tre *)cntxt_rp;
-		dev_dbg(gpii->gpi_dev->dev, "%s: TRE: %08x:%08x:%08x:%08x\n", __func__,
-			tre->dword[0], tre->dword[1], tre->dword[2],
-			tre->dword[3]);
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "%s: GPII:%d TRE: %08x:%08x:%08x:%08x\n", __func__,
+			     gpii->gpii_id, tre->dword[0], tre->dword[1], tre->dword[2],
+			     tre->dword[3]);
 
 		if (tre->dword[3] & TRE_FLAGS_CCU_LAST)
 			last_frame = tre;
@@ -1914,9 +2002,10 @@ static void gpi_nop_tre(struct gpii *gpii, struct gchan *gchan)
 		if (last_frame)
 			last_frame->dword[3] |= u32_encode_bits(1, TRE_FLAGS_NOP_LAST);
 
-		dev_dbg(gpii->gpi_dev->dev, "chid:%d, rp:0x%llx, wp:0x%llx, TRE: %08x:%08x:%08x:%08x\n",
-			gchan->chid, local_rp, local_wp, tre->dword[0], tre->dword[1],
-			tre->dword[2], tre->dword[3]);
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "GPII:%d chid:%d, rp:0x%llx, wp:0x%llx, TRE: %08x:%08x:%08x:%08x\n",
+			    gpii->gpii_id, gchan->chid, local_rp, local_wp, tre->dword[0],
+			    tre->dword[1], tre->dword[2], tre->dword[3]);
 	}
 }
 
@@ -1935,8 +2024,8 @@ static int gpi_terminate_all(struct dma_chan *chan)
 	if (gpii->premature_cancel) {
 		ret = gpi_send_cmd(gpii, gchan, GPI_CH_CMD_SYNC_TRIGGER_DIS);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
-				TO_GPI_CMD_STR(GPI_CH_CMD_SYNC_TRIGGER_DIS), ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
+				    TO_GPI_CMD_STR(GPI_CH_CMD_SYNC_TRIGGER_DIS), ret);
 			goto terminate_exit;
 		}
 	}
@@ -1975,14 +2064,16 @@ static int gpi_terminate_all(struct dma_chan *chan)
 
 		ret = gpi_reset_chan(gchan, GPI_CH_CMD_RESET);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error resetting channel ret:%d\n", ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "Error resetting channel ret:%d\n", ret);
 			goto terminate_exit;
 		}
 
 		/* reprogram channel CNTXT */
 		ret = gpi_alloc_chan(gchan, false);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error alloc_channel ret:%d\n", ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "Error alloc_channel ret:%d\n", ret);
 			goto terminate_exit;
 		}
 	}
@@ -2001,7 +2092,8 @@ static int gpi_terminate_all(struct dma_chan *chan)
 
 		ret = gpi_start_chan(gchan);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error Starting Channel ret:%d\n", ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "Error Starting Channel ret:%d\n", ret);
 			goto terminate_exit;
 		}
 	}
@@ -2010,8 +2102,8 @@ static int gpi_terminate_all(struct dma_chan *chan)
 	if (gpii->premature_cancel) {
 		ret = gpi_send_cmd(gpii, gchan, GPI_CH_CMD_SYNC_TRIGGER_EN);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
-				TO_GPI_CMD_STR(GPI_CH_CMD_SYNC_TRIGGER_EN), ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "Error with cmd:%s ret:%d\n",
+				    TO_GPI_CMD_STR(GPI_CH_CMD_SYNC_TRIGGER_EN), ret);
 			goto terminate_exit;
 		}
 	}
@@ -2045,14 +2137,16 @@ static int gpi_channels_configure(struct dma_chan *chan)
 
 	ret = gpi_config_interrupts(gpii, DEFAULT_IRQ_SETTINGS, 0);
 	if (ret) {
-		dev_dbg(gpii->gpi_dev->dev, "error config. interrupts, ret:%d\n", ret);
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "error config. interrupts, ret:%d\n", ret);
 		return ret;
 	}
 
 	/* allocate event rings */
 	ret = gpi_alloc_ev_chan(gpii);
 	if (ret) {
-		dev_dbg(gpii->gpi_dev->dev, "error alloc_ev_chan:%d\n", ret);
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "error alloc_ev_chan:%d\n", ret);
 		goto error_alloc_ev_ring;
 	}
 
@@ -2060,7 +2154,8 @@ static int gpi_channels_configure(struct dma_chan *chan)
 	for (i = 0; i < MAX_CHANNELS_PER_GPII; i++) {
 		ret = gpi_alloc_chan(&gpii->gchan[i], true);
 		if (ret) {
-			dev_dbg(gpii->gpi_dev->dev, "Error allocating chan:%d\n", ret);
+			GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+				    "Error allocating chan:%d\n", ret);
 			goto error_alloc_chan;
 		}
 	}
@@ -2070,7 +2165,8 @@ static int gpi_channels_configure(struct dma_chan *chan)
 		/* Issue channel START command (1st for RX, followed by TX) */
 		ret = gpi_start_chan(&gpii->gchan[i]);
 		if (ret) {
-			dev_dbg(gpii->gpi_dev->dev, "Error start chan:%d\n", ret);
+			GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+				    "Error start chan:%d\n", ret);
 			goto error_start_chan;
 		}
 	}
@@ -2133,7 +2229,8 @@ static int gpi_pause(struct dma_chan *chan)
 	 * client needs to call pause only once
 	 */
 	if (gpii->pm_state == PAUSE_STATE) {
-		dev_dbg(gpii->gpi_dev->dev, "channel is already paused\n");
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "channel is already paused for GPII:%d\n", gpii->gpii_id);
 		mutex_unlock(&gpii->ctrl_lock);
 		return 0;
 	}
@@ -2150,7 +2247,8 @@ static int gpi_pause(struct dma_chan *chan)
 	if (gpi_ctrl && gpi_ctrl->cmd == QCOM_GPI_CHANNEL_DECONFIGURE) {
 		ret = gpi_channels_deconfigure(chan);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error deconfiguring chan, ret:%d\n", ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "Error deconfiguring chan, ret:%d\n", ret);
 			mutex_unlock(&gpii->ctrl_lock);
 			return ret;
 		}
@@ -2180,7 +2278,8 @@ static int gpi_resume(struct dma_chan *chan)
 
 	mutex_lock(&gpii->ctrl_lock);
 	if (gpii->pm_state == ACTIVE_STATE) {
-		dev_dbg(gpii->gpi_dev->dev, "channel is already active\n");
+		GPI_DBG_LOG(LOG_LVL_INFO, gpii->gpi_dev->dev,
+			    "channel is already active for GPII:%d\n", gpii->gpii_id);
 		mutex_unlock(&gpii->ctrl_lock);
 		return 0;
 	}
@@ -2191,7 +2290,8 @@ static int gpi_resume(struct dma_chan *chan)
 	if (gpi_ctrl && gpi_ctrl->cmd == QCOM_GPI_CHANNEL_CONFIGURE) {
 		ret = gpi_channels_configure(chan);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error re-configuring chan, ret:%d\n", ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "Error re-configuring chan, ret:%d\n", ret);
 			mutex_unlock(&gpii->ctrl_lock);
 			return ret;
 		}
@@ -2206,7 +2306,8 @@ static int gpi_resume(struct dma_chan *chan)
 	for (i = 0; i < MAX_CHANNELS_PER_GPII; i++) {
 		ret = gpi_send_cmd(gpii, &gpii->gchan[i], GPI_CH_CMD_START);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error starting chan, ret:%d\n", ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "Error starting chan:%d, ret:%d\n", i, ret);
 			mutex_unlock(&gpii->ctrl_lock);
 			return ret;
 		}
@@ -2387,8 +2488,8 @@ static int gpi_create_i2c_tre(struct gchan *chan, struct gpi_desc *desc,
 	}
 
 	for (i = 0; i < tre_idx; i++)
-		dev_dbg(dev, "TRE:%d %x:%x:%x:%x\n", i, desc->tre[i].dword[0],
-			desc->tre[i].dword[1], desc->tre[i].dword[2], desc->tre[i].dword[3]);
+		GPI_DBG_LOG(LOG_LVL_INFO, dev, "TRE:%d %x:%x:%x:%x\n", i, desc->tre[i].dword[0],
+			    desc->tre[i].dword[1], desc->tre[i].dword[2], desc->tre[i].dword[3]);
 
 	return tre_idx;
 }
@@ -2484,8 +2585,8 @@ static int gpi_create_spi_tre(struct gchan *chan, struct gpi_desc *desc,
 	}
 
 	for (i = 0; i < tre_idx; i++)
-		dev_dbg(dev, "TRE:%d %x:%x:%x:%x\n", i, desc->tre[i].dword[0],
-			desc->tre[i].dword[1], desc->tre[i].dword[2], desc->tre[i].dword[3]);
+		GPI_DBG_LOG(LOG_LVL_INFO, dev, "TRE:%d %x:%x:%x:%x\n", i, desc->tre[i].dword[0],
+			    desc->tre[i].dword[1], desc->tre[i].dword[2], desc->tre[i].dword[3]);
 
 	return tre_idx;
 }
@@ -2507,12 +2608,14 @@ gpi_prep_slave_sg(struct dma_chan *chan, struct scatterlist *sgl,
 
 	gpii->ieob_set = false;
 	if (!is_slave_direction(direction)) {
-		dev_err(gpii->gpi_dev->dev, "invalid dma direction: %d\n", direction);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "invalid dma direction: %d\n", direction);
 		return NULL;
 	}
 
 	if (sg_len > 1) {
-		dev_err(dev, "Multi sg sent, we support only one atm: %d\n", sg_len);
+		GPI_ERR_LOG(LOG_LVL_ERROR, dev,
+			    "Multi sg sent, we support only one atm: %d\n", sg_len);
 		return NULL;
 	}
 
@@ -2526,7 +2629,8 @@ gpi_prep_slave_sg(struct dma_chan *chan, struct scatterlist *sgl,
 	/* calculate # of elements required & available */
 	nr = gpi_ring_num_elements_avail(ch_ring);
 	if (nr < nr_tre) {
-		dev_err(dev, "not enough space in ring, avail:%u required:%u\n", nr, nr_tre);
+		GPI_ERR_LOG(LOG_LVL_ERROR, dev,
+			    "not enough space in ring, avail:%u required:%u\n", nr, nr_tre);
 		return NULL;
 	}
 
@@ -2540,7 +2644,7 @@ gpi_prep_slave_sg(struct dma_chan *chan, struct scatterlist *sgl,
 	} else if (gchan->protocol == QCOM_GPI_I2C) {
 		i = gpi_create_i2c_tre(gchan, gpi_desc, sgl, direction);
 	} else {
-		dev_err(dev, "invalid peripheral: %d\n", gchan->protocol);
+		GPI_ERR_LOG(LOG_LVL_ERROR, dev, "invalid peripheral: %d\n", gchan->protocol);
 		kfree(gpi_desc);
 		return NULL;
 	}
@@ -2607,8 +2711,9 @@ static int gpi_ch_init(struct gchan *gchan)
 
 	/* protocol must be same for both channels */
 	if (gpii->gchan[0].protocol != gpii->gchan[1].protocol) {
-		dev_err(gpii->gpi_dev->dev, "protocol did not match protocol %u != %u\n",
-			gpii->gchan[0].protocol, gpii->gchan[1].protocol);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "protocol did not match protocol %u != %u\n",
+			    gpii->gchan[0].protocol, gpii->gchan[1].protocol);
 		ret = -EINVAL;
 		goto exit_gpi_init;
 	}
@@ -2626,14 +2731,15 @@ static int gpi_ch_init(struct gchan *gchan)
 	write_unlock_irq(&gpii->pm_lock);
 	ret = gpi_config_interrupts(gpii, DEFAULT_IRQ_SETTINGS, 0);
 	if (ret) {
-		dev_err(gpii->gpi_dev->dev, "error config. interrupts, ret:%d\n", ret);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+			    "error config. interrupts, ret:%d\n", ret);
 		goto error_config_int;
 	}
 
 	/* allocate event rings */
 	ret = gpi_alloc_ev_chan(gpii);
 	if (ret) {
-		dev_err(gpii->gpi_dev->dev, "error alloc_ev_chan:%d\n", ret);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev, "error alloc_ev_chan:%d\n", ret);
 		goto error_alloc_ev_ring;
 	}
 
@@ -2641,7 +2747,8 @@ static int gpi_ch_init(struct gchan *gchan)
 	for (i = 0; i < MAX_CHANNELS_PER_GPII; i++) {
 		ret = gpi_alloc_chan(&gpii->gchan[i], true);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error allocating chan:%d\n", ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "Error allocating chan:%d\n", ret);
 			goto error_alloc_chan;
 		}
 	}
@@ -2650,7 +2757,8 @@ static int gpi_ch_init(struct gchan *gchan)
 	for (i = 0; i < MAX_CHANNELS_PER_GPII; i++) {
 		ret = gpi_start_chan(&gpii->gchan[i]);
 		if (ret) {
-			dev_err(gpii->gpi_dev->dev, "Error start chan:%d\n", ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "Error start chan:%d\n", ret);
 			goto error_start_chan;
 		}
 	}
@@ -2696,7 +2804,8 @@ static void gpi_free_chan_resources(struct dma_chan *chan)
 
 		ret = gpi_send_cmd(gpii, gchan, GPI_CH_CMD_RESET);
 		if (ret)
-			dev_err(gpii->gpi_dev->dev, "error resetting channel:%d\n", ret);
+			GPI_ERR_LOG(LOG_LVL_ERROR, gpii->gpi_dev->dev,
+				    "error resetting channel:%d\n", ret);
 
 		gpi_reset_chan(gchan, GPI_CH_CMD_DE_ALLOC);
 	}
@@ -2828,41 +2937,43 @@ static struct dma_chan *gpi_of_dma_xlate(struct of_phandle_args *args,
 	struct gchan *gchan;
 
 	if (args->args_count < 3) {
-		dev_err(gpi_dev->dev, "gpii require minimum 2 args, client passed:%d args\n",
-			args->args_count);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpi_dev->dev,
+			    "gpii require minimum 2 args, client passed:%d args\n",
+			    args->args_count);
 		return NULL;
 	}
 
 	chid = args->args[0];
 	if (chid >= MAX_CHANNELS_PER_GPII) {
-		dev_err(gpi_dev->dev, "gpii channel:%d not valid\n", chid);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpi_dev->dev, "gpii channel:%d not valid\n", chid);
 		return NULL;
 	}
 
 	seid = args->args[1];
 
 	static_gpii_no = (args->args[3] & STATIC_GPII_BMASK);
-	dev_dbg(gpi_dev->dev, "static gpii no:%d\n", args->args[3]);
+	GPI_DBG_LOG(LOG_LVL_INFO, gpi_dev->dev, "static gpii no:%d\n", args->args[3]);
 
 	if (static_gpii_no) {
 		gpii = gpi_find_static_gpii(gpi_dev, (static_gpii_no - 1));
-		dev_dbg(gpi_dev->dev, "Static GPII allocation req:%d Got:%d\n",
-			(static_gpii_no - 1), gpii);
+		GPI_DBG_LOG(LOG_LVL_INFO, gpi_dev->dev, "Static GPII allocation req:%d Got:%d\n",
+			    (static_gpii_no - 1), gpii);
 	} else {
 		/* find next available gpii to use */
 		gpii = gpi_find_avail_gpii(gpi_dev, seid);
-		dev_dbg(gpi_dev->dev, "Dynamic GPII allocation - Got:%d\n", gpii);
+		GPI_DBG_LOG(LOG_LVL_INFO, gpi_dev->dev, "Dynamic GPII allocation - Got:%d\n", gpii);
 	}
 
 	if (gpii < 0) {
-		dev_err(gpi_dev->dev, "no available gpii instances\n");
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpi_dev->dev, "no available gpii instances\n");
 		return NULL;
 	}
 
 	gchan = &gpi_dev->gpiis[gpii].gchan[chid];
 	if (gchan->vc.chan.client_count) {
-		dev_err(gpi_dev->dev, "gpii:%d chid:%d seid:%d already configured\n",
-			gpii, chid, gchan->seid);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpi_dev->dev,
+			    "gpii:%d chid:%d seid:%d already configured\n",
+			    gpii, chid, gchan->seid);
 		return NULL;
 	}
 
@@ -2892,14 +3003,14 @@ static int gpi_probe(struct platform_device *pdev)
 	ret = of_property_read_u32(gpi_dev->dev->of_node, "dma-channels",
 				   &gpi_dev->max_gpii);
 	if (ret) {
-		dev_err(gpi_dev->dev, "missing 'max-no-gpii' DT node\n");
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpi_dev->dev, "missing 'max-no-gpii' DT node\n");
 		return ret;
 	}
 
 	ret = of_property_read_u32(gpi_dev->dev->of_node, "dma-channel-mask",
 				   &gpi_dev->gpii_mask);
 	if (ret) {
-		dev_err(gpi_dev->dev, "missing 'gpii-mask' DT node\n");
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpi_dev->dev, "missing 'gpii-mask' DT node\n");
 		return ret;
 	}
 
@@ -2910,7 +3021,7 @@ static int gpi_probe(struct platform_device *pdev)
 	if (data->is_ccu_qup) {
 		gpi_dev->cfg = *data;
 		register_gpi_irq(ccu_gpi_handle_isr);
-		dev_dbg(gpi_dev->dev, "Loading CCU QUP FW\n");
+		GPI_DBG_LOG(LOG_LVL_INFO, gpi_dev->dev, "Loading CCU QUP FW\n");
 		ccu_qup_load_firmware();
 	}
 
@@ -2920,11 +3031,12 @@ static int gpi_probe(struct platform_device *pdev)
 	ret = of_property_read_u32(gpi_dev->dev->of_node, "static-gpii-mask",
 				   &gpi_dev->static_gpii_mask);
 	if (!ret)
-		dev_err(gpi_dev->dev, "Static GPII use case\n");
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpi_dev->dev, "Static GPII use case\n");
 
 	ret = dma_set_mask(gpi_dev->dev, DMA_BIT_MASK(64));
 	if (ret) {
-		dev_err(gpi_dev->dev, "Error setting dma_mask to 64, ret:%d\n", ret);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpi_dev->dev,
+			    "Error setting dma_mask to 64, ret:%d\n", ret);
 		return ret;
 	}
 
@@ -3019,17 +3131,23 @@ static int gpi_probe(struct platform_device *pdev)
 	/* register with dmaengine framework */
 	ret = dma_async_device_register(&gpi_dev->dma_device);
 	if (ret) {
-		dev_err(gpi_dev->dev, "async_device_register failed ret:%d", ret);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpi_dev->dev,
+			    "async_device_register failed ret:%d", ret);
 		return ret;
 	}
 
 	ret = of_dma_controller_register(gpi_dev->dev->of_node,
 					 gpi_of_dma_xlate, gpi_dev);
 	if (ret) {
-		dev_err(gpi_dev->dev, "of_dma_controller_reg failed ret:%d", ret);
+		GPI_ERR_LOG(LOG_LVL_ERROR, gpi_dev->dev,
+			    "of_dma_controller_reg failed ret:%d", ret);
 		return ret;
 	}
 
+	ret = device_create_file(gpi_dev->dev, &dev_attr_log_level);
+	if (ret)
+		dev_err(&pdev->dev, "Failed to create sysfs entry ret:%d\n", ret);
+
 	return ret;
 }
 
diff --git a/drivers/i2c/busses/i2c-qcom-geni.c b/drivers/i2c/busses/i2c-qcom-geni.c
index 407a124939c2..3fd13a6930c4 100644
--- a/drivers/i2c/busses/i2c-qcom-geni.c
+++ b/drivers/i2c/busses/i2c-qcom-geni.c
@@ -22,22 +22,38 @@
 #include <soc/qcom/qup_fw_load.h>
 
 #define CREATE_TRACE_POINTS
-#include <trace/events/qup_buses_trace.h>
+#include <trace/events/qup_i2c_trace.h>
+
+enum DEBUG_LOG_LVL {
+	LOG_LVL_ERROR,
+	LOG_LVL_INFO,
+	LOG_LVL_VERBOSE,
+	LOG_LVL_MAX,
+};
 
 void i2c_trace_log(struct device *dev, const char *fmt, ...)
 {
-	struct va_format vaf = {
-		.fmt = fmt,
-	};
-
 	va_list args;
+	struct va_format vaf;
 
 	va_start(args, fmt);
+	vaf.fmt = fmt;
 	vaf.va = &args;
-	trace_buses_log_info(dev_name(dev), &vaf);
+
+	trace_i2c_log_info(dev_name(dev), &vaf);
 	va_end(args);
 }
 
+#define I2C_LOG(log_fn, level, dev, fmt, ...) do { \
+	if ((level) <= log_level) \
+		i2c_trace_log(dev, fmt, ##__VA_ARGS__); \
+	else \
+		log_fn(dev, fmt, ##__VA_ARGS__); \
+} while (0)
+
+#define I2C_ERR_LOG(level, dev, fmt, ...) I2C_LOG(dev_err, level, dev, fmt, ##__VA_ARGS__)
+#define I2C_DBG_LOG(level, dev, fmt, ...) I2C_LOG(dev_dbg, level, dev, fmt, ##__VA_ARGS__)
+
 #define SE_I2C_TX_TRANS_LEN		0x26c
 #define SE_I2C_RX_TRANS_LEN		0x270
 #define SE_I2C_SCL_COUNTERS		0x278
@@ -316,6 +332,27 @@ static void release_gpi_dma(struct geni_i2c_dev *gi2c);
 static void i2c_gpi_cb_result(void *cb, const struct dmaengine_result *result);
 static void geni_i2c_gpi_release_ccu_frame(struct geni_i2c_dev *gi2c, struct ccu_frame_info *frame);
 
+static int log_level = -1;
+static ssize_t log_level_show(struct device *dev,
+			      struct device_attribute *attr, char *buf)
+{
+	return snprintf(buf, PAGE_SIZE, "%d\n", log_level);
+}
+
+static ssize_t log_level_store(struct device *dev,
+			       struct device_attribute *attr,
+			       const char *buf, size_t count)
+{
+	int val;
+
+	if (kstrtoint(buf, 10, &val) != 0 || val < LOG_LVL_ERROR || val >= LOG_LVL_MAX)
+		return -EINVAL;
+
+	log_level = val;
+	return count;
+}
+static DEVICE_ATTR_RW(log_level);
+
 static int geni_i2c_clk_map_idx(struct geni_i2c_dev *gi2c)
 {
 	unsigned int i;
@@ -384,6 +421,8 @@ static int geni_i2c_power_on_off(struct i2c_adapter *adap, bool suspend)
 			return -EINVAL;
 		}
 	}
+
+	I2C_DBG_LOG(LOG_LVL_INFO, gi2c->se.dev, "%s: suspend:%d\n", __func__, suspend);
 	if (suspend) {
 		for (i = 0; i < CCU_MAX_GPII; i++) {
 			if (gi2c->tx_c[i]) {
@@ -391,8 +430,8 @@ static int geni_i2c_power_on_off(struct i2c_adapter *adap, bool suspend)
 				gi2c->tx_c[i]->private = &gi2c->tx_gchan_ctrl;
 				ret = dmaengine_pause(gi2c->tx_c[i]);
 				if (ret) {
-					dev_err(gi2c->se.dev, "dmaengine_pause failed: ret:%d\n",
-						ret);
+					I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+						    "dmaengine_pause failed: ret:%d\n", ret);
 					return -EAGAIN;
 				}
 			}
@@ -402,7 +441,8 @@ static int geni_i2c_power_on_off(struct i2c_adapter *adap, bool suspend)
 			/* Decrement the device usage counter and call pm runtime idle */
 			ret = pm_runtime_put_sync(gi2c->se.dev);
 			if (ret < 0) {
-				dev_err(gi2c->se.dev, "error turning off device :%d\n", ret);
+				I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+					    "error turning off device :%d\n", ret);
 				return ret;
 			}
 		}
@@ -410,7 +450,8 @@ static int geni_i2c_power_on_off(struct i2c_adapter *adap, bool suspend)
 		/* Increment the device usage counter and resume */
 		ret = pm_runtime_resume_and_get(gi2c->se.dev);
 		if (ret < 0) {
-			dev_err(gi2c->se.dev, "error turning on device :%d\n", ret);
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+				    "error turning on device :%d\n", ret);
 			return ret;
 		}
 
@@ -420,8 +461,8 @@ static int geni_i2c_power_on_off(struct i2c_adapter *adap, bool suspend)
 				gi2c->tx_c[i]->private = &gi2c->tx_gchan_ctrl;
 				ret = dmaengine_resume(gi2c->tx_c[i]);
 				if (ret) {
-					dev_err(gi2c->se.dev, "dmaengine_resume failed: ret:%d\n",
-						ret);
+					I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+						    "dmaengine_resume failed: ret:%d\n", ret);
 					return -EAGAIN;
 				}
 			}
@@ -456,14 +497,15 @@ static int geni_i2c_gpi_send_bus_cmd(struct geni_i2c_dev *gi2c, u8 cmd)
 
 	ret = dmaengine_slave_config(dma_chan, &config);
 	if (ret) {
-		dev_err(gi2c->se.dev, "dma config error: %d for op:%d\n", ret, cmd);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "dma config error: %d for op:%d\n", ret, cmd);
 		return ret;
 	}
 
 	desc = dmaengine_prep_slave_single(dma_chan, NULL, 0, DMA_MEM_TO_DEV,
 					   DMA_PREP_INTERRUPT | DMA_CTRL_ACK);
 	if (!desc) {
-		dev_err(gi2c->se.dev, "%s: prep_slave_sg failed\n", __func__);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "%s: prep_slave_sg failed\n", __func__);
 		return -EIO;
 	}
 
@@ -471,8 +513,8 @@ static int geni_i2c_gpi_send_bus_cmd(struct geni_i2c_dev *gi2c, u8 cmd)
 	desc->callback_param = &peripheral;
 	cookie = dmaengine_submit(desc);
 	if (dma_submit_error(cookie)) {
-		dev_err(gi2c->se.dev, "%s: dmaengine_submit failed (%d)\n",
-			__func__, cookie);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "%s: dmaengine_submit failed (%d)\n",
+			    __func__, cookie);
 		dmaengine_desc_free(desc);
 		return -EIO;
 	}
@@ -482,11 +524,13 @@ static int geni_i2c_gpi_send_bus_cmd(struct geni_i2c_dev *gi2c, u8 cmd)
 	reinit_completion(&gi2c->done);
 	time_left = wait_for_completion_timeout(&gi2c->done, XFER_TIMEOUT);
 	if (!time_left) {
-		dev_err(gi2c->se.dev, "%s: I2C timeout for cmd:%d\n", __func__, cmd);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "%s: I2C timeout for cmd:%d\n", __func__, cmd);
 		gi2c->err = -ETIMEDOUT;
 		dmaengine_terminate_sync(dma_chan);
 	}
 
+	I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "CMD:%d send successfully\n", cmd);
 	return gi2c->err;
 }
 
@@ -507,11 +551,11 @@ static int geni_i2c_send_cmd_fifo_mode(struct geni_i2c_dev *gi2c, int cmd)
 	geni_se_setup_m_cmd(&gi2c->se, cmd, m_param);
 	time_left = wait_for_completion_timeout(&gi2c->done, XFER_TIMEOUT);
 	if (!time_left) {
-		dev_err(gi2c->se.dev, "I2C timeout for cmd:%d\n", cmd);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "I2C timeout for cmd:%d\n", cmd);
 		return -ETIMEDOUT;
 	}
 
-	dev_dbg(gi2c->se.dev, "CMD:%d send successfully\n", cmd);
+	I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "CMD:%d send successfully\n", cmd);
 	return 0;
 }
 
@@ -558,34 +602,38 @@ int geni_i2c_check_recover_ios_line(struct i2c_adapter *adap)
 	}
 
 	if (geni_i2c_is_bus_recovery_required(gi2c)) {
-		dev_err(gi2c->se.dev, "IOS lines in bad state, need recovery\n");
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "IOS lines in bad state, need recovery\n");
 	} else {
-		dev_dbg(gi2c->se.dev, "IOS lines in good state\n");
+		I2C_DBG_LOG(LOG_LVL_INFO, gi2c->se.dev, "IOS lines in good state\n");
 		return 0;
 	}
 
 	if (gi2c->xfer_mode == GENI_GPI_DMA) {
 		ret = geni_i2c_gpi_send_bus_cmd(gi2c, I2C_BUS_CLEAR);
 		if (ret) {
-			dev_err(gi2c->se.dev, "BUS CLEAR cmd failed for GPI mode\n");
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+				    "BUS CLEAR cmd failed for GPI mode\n");
 			return ret;
 		}
 
 		ret = geni_i2c_gpi_send_bus_cmd(gi2c, I2C_STOP_ON_BUS);
 		if (ret) {
-			dev_err(gi2c->se.dev, "STOP_ON_BUS cmd failed for GPI mode\n");
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+				    "STOP_ON_BUS cmd failed for GPI mode\n");
 			return ret;
 		}
 	} else {
 		ret = geni_i2c_send_cmd_fifo_mode(gi2c, I2C_BUS_CLEAR);
 		if (ret) {
-			dev_err(gi2c->se.dev, "BUS Clear cmd failed for FIFO mode\n");
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+				    "BUS Clear cmd failed for FIFO mode\n");
 			return ret;
 		}
 
 		ret = geni_i2c_send_cmd_fifo_mode(gi2c, I2C_STOP_ON_BUS);
 		if (ret) {
-			dev_err(gi2c->se.dev, "STOP_ON_BUS cmd failed for FIFO mode\n");
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+				    "STOP_ON_BUS cmd failed for FIFO mode\n");
 			return ret;
 		}
 	}
@@ -622,17 +670,21 @@ static int geni_i2c_configure_ccu_mode(struct i2c_adapter *adap, int gpii_no,
 	}
 
 	if (gpii_no > CCU_MAX_GPII_NUM) {
-		dev_err(gi2c->se.dev, "%s: Invalid request of GPII:%d\n", __func__, gpii_no);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "%s: Invalid request of GPII:%d\n", __func__, gpii_no);
 		return -EINVAL;
 	}
 	idx = (gpii_no % CCU_MAX_GPII);
 
 	if (ch_type < 0 || ch_type > 2 || idx < 0) {
-		dev_err(gi2c->se.dev, "%s: Invalid request, ch_type:%d, mode:%d gpii_no:%d\n",
-			__func__, ch_type, mode, gpii_no);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "%s: Invalid request, ch_type:%d, mode:%d gpii_no:%d\n",
+			    __func__, ch_type, mode, gpii_no);
 		return -EINVAL;
 	}
 
+	I2C_DBG_LOG(LOG_LVL_INFO, gi2c->se.dev, "Configure %d mode and ch_type:%d for GPII%d\n",
+		    mode, ch_type, gpii_no);
 	gi2c->ccu.mode = mode;
 	gi2c->ccu.gpi[idx].type = ch_type;
 	gi2c->ccu.gpi[idx].gpii_mode_change = true;
@@ -640,7 +692,8 @@ static int geni_i2c_configure_ccu_mode(struct i2c_adapter *adap, int gpii_no,
 	if (!gi2c->rx_c[idx] ||  !gi2c->tx_c[idx]) {
 		ret = setup_gpi_dma(gi2c, idx);
 		if (ret) {
-			dev_err(gi2c->se.dev, "%s: Setup gpi dma failed:%d\n", __func__, __LINE__);
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+				    "%s: Setup gpi dma failed:%d\n", __func__, __LINE__);
 			return ret;
 		}
 	}
@@ -685,18 +738,23 @@ static int geni_i2c_gpii_reset(struct i2c_adapter *adap, int gpii_no,
 
 	if (premature_cancel) {
 		if (idx == REGULAR_GPII) {
-			dev_err(gi2c->se.dev, "%s: premature cancel is invalid for a regular channel:%d\n",
-				__func__, gpii_no);
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+				    "%s: premature cancel is invalid for a regular channel:%d\n",
+				    __func__, gpii_no);
 			return -EINVAL;
 		}
 		ccu_gpi_set_premature_cancel_flag(gi2c->tx_c[idx]);
 	}
 
+	I2C_DBG_LOG(LOG_LVL_INFO, gi2c->se.dev,
+		    "GPII Reset operation for GPII%d, premature_cancel:%d\n",
+		    gpii_no, premature_cancel);
 	if (gi2c->tx_c[idx]) {
 		ret = dmaengine_terminate_sync(gi2c->tx_c[idx]);
 		if (ret) {
-			dev_err(gi2c->se.dev, "%s: gpi terminate failed for tx_c[%d] ret:%d\n",
-				__func__, idx, ret);
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+				    "%s: gpi terminate failed for tx_c[%d] ret:%d\n",
+				    __func__, idx, ret);
 			return ret;
 		}
 
@@ -744,15 +802,16 @@ static void geni_i2c_configure_pre_delay(struct i2c_adapter *adap, unsigned long
 	if (((delay > 65535) && (gi2c->clk_freq_out == KHZ(1000))) ||
 		((delay > 163838) && (gi2c->clk_freq_out == KHZ(400))) ||
 		((delay > 655350) && (gi2c->clk_freq_out == KHZ(100)))) {
-			dev_err(gi2c->se.dev, "%s: Invalid delay:%d received for %d frequency\n",
-				__func__, delay, gi2c->clk_freq_out);
-			return;
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "%s: Invalid delay:%d received for %d frequency\n",
+			    __func__, delay, gi2c->clk_freq_out);
+		return;
 	}
 
 	delay_cycle = ((1*NSEC_PER_SEC)/gi2c->clk_freq_out);
 	gi2c->ccu.inter_delay = ((delay *NSEC_PER_USEC)/delay_cycle);
-	dev_dbg(gi2c->se.dev, "%s: Received delay in(Us): %d delay_cycle:%d\n",
-		__func__, delay, gi2c->ccu.inter_delay);
+	I2C_DBG_LOG(LOG_LVL_INFO, gi2c->se.dev, "%s: Received delay in(Us): %d delay_cycle:%d\n",
+		    __func__, delay, gi2c->ccu.inter_delay);
 }
 
 static void geni_i2c_err_misc(struct geni_i2c_dev *gi2c)
@@ -771,10 +830,11 @@ static void geni_i2c_err_misc(struct geni_i2c_dev *gi2c)
 		rx_st = readl_relaxed(gi2c->se.base + SE_GENI_RX_FIFO_STATUS);
 		tx_st = readl_relaxed(gi2c->se.base + SE_GENI_TX_FIFO_STATUS);
 	}
-	i2c_trace_log(gi2c->se.dev, "DMA:%d tx_stat:0x%x, rx_stat:0x%x, irq-stat:0x%x\n",
-		      dma, tx_st, rx_st, m_stat);
-	i2c_trace_log(gi2c->se.dev, "m_cmd:0x%x, geni_status:0x%x, geni_ios:0x%x\n",
-		      m_cmd, geni_s, geni_ios);
+	I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev,
+		    "DMA:%d tx_stat:0x%x, rx_stat:0x%x, irq-stat:0x%x\n",
+		    dma, tx_st, rx_st, m_stat);
+	I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "m_cmd:0x%x, geni_status:0x%x, geni_ios:0x%x\n",
+		    m_cmd, geni_s, geni_ios);
 }
 
 static void geni_i2c_err(struct geni_i2c_dev *gi2c, int err)
@@ -782,8 +842,8 @@ static void geni_i2c_err(struct geni_i2c_dev *gi2c, int err)
 	if (!gi2c->err)
 		gi2c->err = gi2c_log[err].err;
 	if (gi2c->cur)
-		i2c_trace_log(gi2c->se.dev, "len:%d, slv-addr:0x%x, RD/WR:%d\n",
-			      gi2c->cur->len, gi2c->cur->addr, gi2c->cur->flags);
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "len:%d, slv-addr:0x%x, RD/WR:%d\n",
+			    gi2c->cur->len, gi2c->cur->addr, gi2c->cur->flags);
 
 	switch (err) {
 	case GENI_ABORT_DONE:
@@ -791,10 +851,10 @@ static void geni_i2c_err(struct geni_i2c_dev *gi2c, int err)
 		break;
 	case NACK:
 	case GENI_TIMEOUT:
-		i2c_trace_log(gi2c->se.dev, "%s\n", gi2c_log[err].msg);
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "%s\n", gi2c_log[err].msg);
 		break;
 	default:
-		dev_err(gi2c->se.dev, "%s\n", gi2c_log[err].msg);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "%s\n", gi2c_log[err].msg);
 		geni_i2c_err_misc(gi2c);
 		break;
 	}
@@ -843,8 +903,8 @@ static void process_i2c_irq(int irq, void *dev)
 		if (!dma)
 			writel_relaxed(0, base + SE_GENI_TX_WATERMARK_REG);
 	} else if (dma) {
-		i2c_trace_log(gi2c->se.dev, "i2c dma tx:0x%x, dma rx:0x%x\n",
-			      dm_tx_st, dm_rx_st);
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "i2c dma tx:0x%x, dma rx:0x%x\n",
+			    dm_tx_st, dm_rx_st);
 	} else if (cur->flags & I2C_M_RD &&
 		   m_stat & (M_RX_FIFO_WATERMARK_EN | M_RX_FIFO_LAST_EN)) {
 		u32 rxcnt = rx_st & RX_FIFO_WC_MSK;
@@ -939,7 +999,7 @@ static void geni_i2c_abort_xfer(struct geni_i2c_dev *gi2c)
 	} while (!gi2c->abort_done && time_left);
 
 	if (!time_left)
-		dev_err(gi2c->se.dev, "Timeout abort_m_cmd\n");
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "Timeout abort_m_cmd\n");
 }
 
 static void geni_i2c_rx_fsm_rst(struct geni_i2c_dev *gi2c)
@@ -954,7 +1014,7 @@ static void geni_i2c_rx_fsm_rst(struct geni_i2c_dev *gi2c)
 	} while (!(val & RX_RESET_DONE) && time_left);
 
 	if (!(val & RX_RESET_DONE))
-		dev_err(gi2c->se.dev, "Timeout resetting RX_FSM\n");
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "Timeout resetting RX_FSM\n");
 }
 
 static void geni_i2c_tx_fsm_rst(struct geni_i2c_dev *gi2c)
@@ -969,7 +1029,7 @@ static void geni_i2c_tx_fsm_rst(struct geni_i2c_dev *gi2c)
 	} while (!(val & TX_RESET_DONE) && time_left);
 
 	if (!(val & TX_RESET_DONE))
-		dev_err(gi2c->se.dev, "Timeout resetting TX_FSM\n");
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "Timeout resetting TX_FSM\n");
 }
 
 static void geni_i2c_rx_msg_cleanup(struct geni_i2c_dev *gi2c,
@@ -1053,7 +1113,7 @@ static int geni_i2c_gpi_nop_tre(struct geni_i2c_dev *gi2c, struct dma_slave_conf
 
 	ret = dmaengine_slave_config(gi2c->tx_c[idx], config);
 	if (ret) {
-		dev_err(gi2c->se.dev, "dma config error: %d\n", ret);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "dma config error: %d\n", ret);
 		goto err_submit;
 	}
 	peripheral_cfg->ccu_cfg.ccu_last = 0;
@@ -1065,7 +1125,7 @@ static int geni_i2c_gpi_nop_tre(struct geni_i2c_dev *gi2c, struct dma_slave_conf
 
 	desc = dmaengine_prep_slave_single(gi2c->tx_c[idx], NULL, 0, DMA_MEM_TO_DEV, flags);
 	if (!desc) {
-		dev_err(gi2c->se.dev, "prep_slave_sg failed\n");
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "prep_slave_sg failed\n");
 		ret = -EIO;
 		goto err_submit;
 	}
@@ -1075,8 +1135,8 @@ static int geni_i2c_gpi_nop_tre(struct geni_i2c_dev *gi2c, struct dma_slave_conf
 
 	cookie = dmaengine_submit(desc);
 	if (dma_submit_error(cookie)) {
-		dev_err(gi2c->se.dev, "%s: dmaengine_submit failed (%d)\n",
-			__func__, cookie);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "%s: dmaengine_submit failed (%d)\n",
+			    __func__, cookie);
 		dmaengine_desc_free(desc);
 		ret = -EIO;
 		goto err_submit;
@@ -1104,10 +1164,11 @@ static int geni_i2c_gpi_nop_tre(struct geni_i2c_dev *gi2c, struct dma_slave_conf
 
 	kfree(gi2c->ccu.msg[0]);
 	gi2c->ccu.msg[0] = NULL;
+	I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "NOP TRE done\n");
 	return 0;
 
 err:
-	dev_err(gi2c->se.dev, "%s: GPI transfer failed: %d\n", __func__, ret);
+	I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "%s: GPI transfer failed: %d\n", __func__, ret);
 	dmaengine_terminate_sync(gi2c->tx_c[idx]);
 
 err_submit:
@@ -1193,6 +1254,7 @@ static void geni_i2c_gpi_release_ccu_frame(struct geni_i2c_dev *gi2c, struct ccu
 		frame->msg[i].addr = 0;
 	}
 	memset(frame, 0, sizeof(*frame));
+	I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "Release CCU Frame done\n");
 }
 
 /*
@@ -1228,10 +1290,11 @@ static void i2c_gpi_cb_result(void *cb, const struct dmaengine_result *result)
 	} else if (i2c->status & (BIT(ARB_LOST) << 5)) {
 		geni_i2c_err(gi2c, ARB_LOST);
 	} else if (result->result != DMA_TRANS_NOERROR) {
-		dev_err(gi2c->se.dev, "DMA txn failed:%d\n", result->result);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "DMA txn failed:%d\n", result->result);
 		gi2c->err = -EIO;
 	} else if (result->residue) {
-		i2c_trace_log(gi2c->se.dev, "DMA xfer has pending: %d\n", result->residue);
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev,
+			    "DMA xfer has pending: %d\n", result->residue);
 	}
 
 	if (i2c->op == I2C_BUS_CLEAR || i2c->op == I2C_STOP_ON_BUS) {
@@ -1250,8 +1313,10 @@ static void i2c_gpi_cb_result(void *cb, const struct dmaengine_result *result)
 			gi2c->ccu.gpi[gpii_idx].frame[frame_idx].frame_cookie;
 		gi2c->ccu.result.err_no = gi2c->err;
 
-		i2c_trace_log(gi2c->se.dev, "CB Result is for i2c-%d GPII:%d Err:%d cookie:0x%x\n",
-					  gi2c->adap.nr, result->gpii_id, gi2c->err, gi2c->ccu.result.frame_cookie);
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev,
+			    "CB Result is for i2c-%d GPII:%d Err:%d cookie:0x%x\n",
+			    gi2c->adap.nr, result->gpii_id, gi2c->err,
+			    gi2c->ccu.result.frame_cookie);
 		ccu_update_xfer_result(&gi2c->ccu.result);
 		/* Free the dma buffer associated to this frame */
 		geni_i2c_release_buf(gi2c, gpii_idx);
@@ -1310,14 +1375,13 @@ static int geni_i2c_gpi(struct geni_i2c_dev *gi2c, struct i2c_msg *msg,
 		}
 		if (!dma_buf)
 			return -ENOMEM;
-		i2c_trace_log(gi2c->se.dev, "%s: CCU QUP - msg.len:%d - [%d]\n",
-			      __func__, msg->len, len);
 	} else {
 		dma_buf = i2c_get_dma_safe_msg_buf(msg, 1);
 		if (!dma_buf)
 			return -ENOMEM;
-		i2c_trace_log(gi2c->se.dev, "%s: QUP - msg.len:%d\n", __func__, msg->len);
 	}
+	I2C_DBG_LOG(LOG_LVL_INFO, gi2c->se.dev, "%s: msg.len:%d len:%d\n",
+		    __func__, msg->len, len);
 
 	if (op == I2C_WRITE)
 		map_dirn = DMA_TO_DEVICE;
@@ -1370,7 +1434,8 @@ static int geni_i2c_gpi(struct geni_i2c_dev *gi2c, struct i2c_msg *msg,
 
 	ret = dmaengine_slave_config(dma_chan, config);
 	if (ret) {
-		dev_err(gi2c->se.dev, "dma config error: %d for op:%d\n", ret, op);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "dma config error: %d for op:%d\n", ret, op);
 		goto err_config;
 	}
 
@@ -1394,7 +1459,7 @@ static int geni_i2c_gpi(struct geni_i2c_dev *gi2c, struct i2c_msg *msg,
 
 	desc = dmaengine_prep_slave_single(dma_chan, addr, len, dma_dirn, flags);
 	if (!desc) {
-		dev_err(gi2c->se.dev, "prep_slave_sg failed\n");
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "prep_slave_sg failed\n");
 		ret = -EIO;
 		goto err_config;
 	}
@@ -1404,8 +1469,8 @@ static int geni_i2c_gpi(struct geni_i2c_dev *gi2c, struct i2c_msg *msg,
 
 	cookie = dmaengine_submit(desc);
 	if (dma_submit_error(cookie)) {
-		dev_err(gi2c->se.dev, "%s: dmaengine_submit failed (%d)\n",
-			__func__, cookie);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "%s: dmaengine_submit failed (%d)\n",
+			    __func__, cookie);
 		goto err_config;
 	}
 
@@ -1442,26 +1507,28 @@ static int geni_i2c_extract_frame_cfg(struct geni_i2c_dev *gi2c, struct i2c_msg
 	int gpii_idx, frame_idx;
 
 	if (msg->len <= CCU_DATA_OFFSET) {
-		dev_err(gi2c->se.dev, "%s: CCU config data not present. len:%d\n",
-			__func__, msg->len);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "%s: CCU config data not present. len:%d\n", __func__, msg->len);
 		return -EINVAL;
 	}
 
 	if (msg_idx > MAX_NUM_MSGS) {
-		dev_err(gi2c->se.dev, "%s: Can't support %d no of msgs\n", __func__, msg_idx);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "%s: Can't support %d no of msgs\n", __func__, msg_idx);
 		return -EINVAL;
 	}
 
 	msg_buf = kmemdup(msg->buf, CCU_DATA_OFFSET, GFP_KERNEL);
 	if (!msg_buf) {
-		dev_err(gi2c->se.dev, "%s:  Memory allocation failed\n", __func__);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "%s: Memory allocation failed\n", __func__);
 		return -ENOMEM;
 	}
 
 	gi2c->ccu.msg[msg_idx] = kzalloc(sizeof(struct ccu_msg_config), GFP_KERNEL);
 	if (!gi2c->ccu.msg[msg_idx]) {
-		dev_err(gi2c->se.dev, "%s:  Memory allocation failed for msg[%d]\n",
-			__func__, msg_idx);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "%s: Memory allocation failed for msg[%d]\n", __func__, msg_idx);
 		kfree(msg_buf);
 		msg_buf = NULL;
 		return -ENOMEM;
@@ -1470,8 +1537,8 @@ static int geni_i2c_extract_frame_cfg(struct geni_i2c_dev *gi2c, struct i2c_msg
 
 	gi2c->ccu.msg[msg_idx]->gpii_no = CCU_GET_GPII(data);
 	if (gi2c->ccu.msg[msg_idx]->gpii_no > CCU_MAX_GPII_NUM) {
-		dev_err(gi2c->se.dev, "%s: Invalid GPII number:%d", __func__,
-			gi2c->ccu.msg[msg_idx]->gpii_no);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "%s: Invalid GPII number:%d", __func__,
+			    gi2c->ccu.msg[msg_idx]->gpii_no);
 		return -EINVAL;
 	}
 
@@ -1492,11 +1559,11 @@ static int geni_i2c_extract_frame_cfg(struct geni_i2c_dev *gi2c, struct i2c_msg
 		gi2c->ccu.gpi[gpii_idx].frame[frame_idx].frame_cookie =
 				CCU_GET_FRAME_COOKIE(data);
 
-	i2c_trace_log(gi2c->se.dev, "%s: *** Message[%d] configuration ***\n"
-				  "GPII:%d Sync:%d Prio:%d Delay after:%d\n"
-				  "Delay Before:%d ch_last:%d ccu_last:%d\n"
-				  "arb_lost_retry:%d pkt_len:%d\n"
-				  "cookie:0x%x Nop:%d\n", __func__, msg_idx,
+	I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "%s: *** Message[%d] configuration ***\n"
+				  "GPII:%d\t Async:%d\t Prio:%d\t Delay after:%d\t"
+				  "Delay Before:%d\t ch_last:%d\t ccu_last:%d\n"
+				  "arb_lost_retry:%d\t pkt_len:%d\t"
+				  "cookie:0x%x\t Nop:%d\n", __func__, msg_idx,
 				  gi2c->ccu.msg[msg_idx]->gpii_no,
 				  gi2c->ccu.gpi[gpii_idx].frame[frame_idx].async,
 				  gi2c->ccu.msg[msg_idx]->priority,
@@ -1544,13 +1611,12 @@ static int geni_i2c_gpi_xfer(struct geni_i2c_dev *gi2c, struct i2c_msg msgs[], i
 	if (gi2c->is_shared)
 		peripheral->shared_se = true;
 
-	i2c_trace_log(gi2c->se.dev, "%s: num:%d addr:0x%x\n",
-		      __func__, num, msgs[0].addr);
+	I2C_DBG_LOG(LOG_LVL_INFO, gi2c->se.dev, "%s: num:%d addr:0x%x\n",
+		    __func__, num, msgs[0].addr);
 	for (i = 0; i < num; i++) {
 		gi2c->cur = &msgs[i];
 		gi2c->msgs = num;
 		gi2c->err = 0;
-		i2c_trace_log(gi2c->se.dev, "msg[%d].len:%d\n", i, gi2c->cur->len);
 
 		peripheral->first_msg = (i == 0) ? 1 : 0;
 		peripheral->last_msg = (i == num - 1) ? 1 : 0;
@@ -1563,20 +1629,23 @@ static int geni_i2c_gpi_xfer(struct geni_i2c_dev *gi2c, struct i2c_msg msgs[], i
 			frame = &gi2c->ccu.gpi[idx].frame[gi2c->ccu.gpi[idx].recvd_cnt];
 
 			if ((msgs[i].flags & I2C_M_RD) && (gi2c->ccu.msg[i]->pkt_len  > 0)){
-				dev_err(gi2c->se.dev, "Invalid Request for RD msg: Tx Sub pkt len:%d, msg:%d\n",
-					gi2c->ccu.msg[i]->pkt_len, i);
+				I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+					    "Invalid Request for RD msg: Tx Sub pkt len:%d, msg:%d\n",
+					    gi2c->ccu.msg[i]->pkt_len, i);
 					return -EINVAL;
 			}
 
 			if (gi2c->ccu.msg[i]->pkt_len > msgs[i].len) {
-				dev_err(gi2c->se.dev, "Invalid Request: msg[%d].len:%d, Tx sub pkt len:%d",
-					i, gi2c->cur->len, gi2c->ccu.msg[i]->pkt_len);
+				I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+					    "Invalid Request: msg[%d].len:%d, Tx sub pkt len:%d",
+					    i, gi2c->cur->len, gi2c->ccu.msg[i]->pkt_len);
 				return -EINVAL;
 			}
 
 			if (gi2c->ccu.msg[i]->nop_frame) {
 				if (idx == REGULAR_GPII || i != 0) {
-					dev_err(gi2c->se.dev, "Invalid Request: idx:%d, msg:%d\n", idx, i);
+					I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+						    "Invalid Request: idx:%d, msg:%d\n", idx, i);
 					return -EINVAL;
 				}
 
@@ -1587,7 +1656,8 @@ static int geni_i2c_gpi_xfer(struct geni_i2c_dev *gi2c, struct i2c_msg msgs[], i
 					return num;
 			}
 		}
-		i2c_trace_log(gi2c->se.dev, "msg[%d].len:%d GPII:%d\n", i, gi2c->cur->len, idx);
+		I2C_DBG_LOG(LOG_LVL_INFO, gi2c->se.dev, "msg[%d].len:%d GPII:%d flag:%d\n",
+			    i, gi2c->cur->len, idx, msgs[i].flags);
 
 		peripheral->stretch = 0;
 		if (i < num - 1)
@@ -1617,22 +1687,25 @@ static int geni_i2c_gpi_xfer(struct geni_i2c_dev *gi2c, struct i2c_msg msgs[], i
 			/* Reduce the length so actual data provided to client*/
 			if (gi2c->is_ccu_qup)
 				msgs[i].len -= CCU_DATA_OFFSET;
+
 			dma_async_issue_pending(gi2c->rx_c[idx]);
 		}
 
 		/* Prioritize regular chan if Priority bit is set */
 		if (gi2c->is_ccu_qup && gi2c->ccu.msg[i]->priority) {
 			if (idx == REGULAR_GPII) {
-				dev_dbg(gi2c->se.dev, "Prioritize regular ch for msg[%d]\n", i);
+				I2C_DBG_LOG(LOG_LVL_INFO, gi2c->se.dev,
+					    "Prioritize regular ch for msg[%d]\n", i);
 				ret = ccu_gpi_prioritize_chan(gi2c->tx_c[idx]);
 				if (ret) {
-					dev_err(gi2c->se.dev, "Failed to prioritize the msg[%d] ret:%d\n",
-						i, ret);
+					I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+						    "Failed to prioritize the msg[%d] ret:%d\n",
+						    i, ret);
 					goto err;
 				}
 			} else {
-				dev_err(gi2c->se.dev, "Priority bit set for non regular ch. GPII:%d\n",
-					idx);
+				I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+					    "Priority bit set for non regular ch. GPII:%d\n", idx);
 			}
 		}
 
@@ -1692,7 +1765,7 @@ static int geni_i2c_gpi_xfer(struct geni_i2c_dev *gi2c, struct i2c_msg msgs[], i
 	return num;
 
 err:
-	dev_err(gi2c->se.dev, "GPI transfer failed: %d\n", ret);
+	I2C_DBG_LOG(LOG_LVL_INFO, gi2c->se.dev, "GPI transfer failed: %d\n", ret);
 	if (gi2c->is_ccu_qup) {
 		kfree(gi2c->ccu.msg[i]);
 		gi2c->ccu.msg[i] = NULL;
@@ -1711,8 +1784,8 @@ static int geni_i2c_fifo_xfer(struct geni_i2c_dev *gi2c,
 {
 	int i, ret = 0;
 
-	i2c_trace_log(gi2c->se.dev, "%s: num:%d addr:0x%x\n",
-		      __func__, num, msgs[0].addr);
+	I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "%s: num:%d addr:0x%x\n",
+		    __func__, num, msgs[0].addr);
 	for (i = 0; i < num; i++) {
 		u32 m_param = i < (num - 1) ? STOP_STRETCH : 0;
 
@@ -1720,12 +1793,12 @@ static int geni_i2c_fifo_xfer(struct geni_i2c_dev *gi2c,
 
 		gi2c->cur = &msgs[i];
 		if (msgs[i].flags & I2C_M_RD) {
-			i2c_trace_log(gi2c->se.dev, "%s: msgs[%d].len:%d R\n",
-				      __func__, i, gi2c->cur->len);
+			I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "%s: msgs[%d].len:%d R\n",
+				    __func__, i, gi2c->cur->len);
 			ret = geni_i2c_rx_one_msg(gi2c, &msgs[i], m_param);
 		} else {
-			i2c_trace_log(gi2c->se.dev, "%s: msgs[%d].len:%d W\n",
-				      __func__, i, gi2c->cur->len);
+			I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "%s: msgs[%d].len:%d W\n",
+				    __func__, i, gi2c->cur->len);
 			ret = geni_i2c_tx_one_msg(gi2c, &msgs[i], m_param);
 		}
 		if (ret)
@@ -1747,11 +1820,13 @@ static int geni_i2c_xfer(struct i2c_adapter *adap,
 	reinit_completion(&gi2c->done);
 	ret = pm_runtime_get_sync(gi2c->se.dev);
 	if (ret < 0 && gi2c->suspended) {
-		i2c_trace_log(gi2c->se.dev, "%s: pm_runtime_get_sync failed ! doing force resume\n",
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev,
+			    "%s: pm_runtime_get_sync failed ! doing force resume\n",
 			      __func__);
 		ret = geni_i2c_runtime_resume(gi2c->se.dev);
 		if (ret) {
-			dev_err(gi2c->se.dev, "error turning SE resources:%d\n", ret);
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+				    "error turning SE resources:%d\n", ret);
 			pm_runtime_put_noidle(gi2c->se.dev);
 			/* Set device in suspended since resume failed */
 			pm_runtime_set_suspended(gi2c->se.dev);
@@ -1824,12 +1899,14 @@ static int setup_gpi_dma(struct geni_i2c_dev *gi2c, int idx)
 	const char *rx_ch[CCU_MAX_GPII] = {"rx", "rx1", "rx2"};
 
 	if (idx >= CCU_MAX_GPII) {
-		dev_err(gi2c->se.dev, "%s: Invalid channel request:%d\n", __func__, idx);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "%s: Invalid channel request:%d\n", __func__, idx);
 		return -EINVAL;
 	}
 
 	if (!gi2c->tx_c[idx]) {
-		dev_dbg(gi2c->se.dev, "%s:[%d] Request TX chan:%s\n", __func__, idx, tx_ch[idx]);
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev,
+			    "%s:[%d] Request TX chan:%s\n", __func__, idx, tx_ch[idx]);
 		gi2c->tx_c[idx] = dma_request_chan(gi2c->se.dev, tx_ch[idx]);
 		if (IS_ERR(gi2c->tx_c[idx])) {
 			ret = dev_err_probe(gi2c->se.dev, PTR_ERR(gi2c->tx_c[idx]),
@@ -1839,7 +1916,8 @@ static int setup_gpi_dma(struct geni_i2c_dev *gi2c, int idx)
 	}
 
 	if (!gi2c->rx_c[idx]) {
-		dev_dbg(gi2c->se.dev, "%s:[%d] Request RX chan:%s\n", __func__, idx, rx_ch[idx]);
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev,
+			    "%s:[%d] Request RX chan:%s\n", __func__, idx, rx_ch[idx]);
 		gi2c->rx_c[idx] = dma_request_chan(gi2c->se.dev, rx_ch[idx]);
 		if (IS_ERR(gi2c->rx_c[idx])) {
 			ret = dev_err_probe(gi2c->se.dev, PTR_ERR(gi2c->rx_c[idx]),
@@ -1850,7 +1928,7 @@ static int setup_gpi_dma(struct geni_i2c_dev *gi2c, int idx)
 		}
 	}
 
-	dev_err(gi2c->se.dev, "Grabbed GPI dma channels\n");
+	I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "Grabbed GPI dma channels\n");
 	return 0;
 }
 
@@ -1950,7 +2028,7 @@ static int geni_i2c_init(struct geni_i2c_dev *gi2c)
 
 	ret = pm_runtime_get_sync(gi2c->se.dev);
 	if (ret < 0) {
-		dev_err(gi2c->se.dev, "error turning on device :%d\n", ret);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "error turning on device :%d\n", ret);
 		return ret;
 	}
 
@@ -1972,12 +2050,13 @@ static int geni_i2c_init(struct geni_i2c_dev *gi2c)
 		for (i = 0; i < idx; i++) {
 			ret = setup_gpi_dma(gi2c, i);
 			if (ret) {
-				dev_err(gi2c->se.dev, "Failed to setup GPI DMA mode\n");
+				I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+					    "Failed to setup GPI DMA mode\n");
 				release_gpi_dma(gi2c);
 				goto err;
 			}
 		}
-		i2c_trace_log(gi2c->se.dev, "Using GPI DMA mode for I2C\n");
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev, "Using GPI DMA mode for I2C\n");
 	} else {
 		gi2c->xfer_mode = GENI_SE_FIFO;
 		tx_depth = geni_se_get_tx_fifo_depth(&gi2c->se);
@@ -1987,7 +2066,7 @@ static int geni_i2c_init(struct geni_i2c_dev *gi2c)
 			tx_depth = desc->tx_fifo_depth;
 
 		if (!tx_depth) {
-			dev_err(gi2c->se.dev, "Invalid TX FIFO depth\n");
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "Invalid TX FIFO depth\n");
 			ret = -EINVAL;
 			goto err;
 		}
@@ -1997,12 +2076,13 @@ static int geni_i2c_init(struct geni_i2c_dev *gi2c)
 		geni_se_config_packing(&gi2c->se, BITS_PER_BYTE,
 				       PACKING_BYTES_PW, true, true, true);
 
-		i2c_trace_log(gi2c->se.dev, "i2c fifo/se-dma mode. fifo depth:%d\n", tx_depth);
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, gi2c->se.dev,
+			    "i2c fifo/se-dma mode. fifo depth:%d\n", tx_depth);
 	}
 
 err:
 	if (pm_runtime_put_sync(gi2c->se.dev) < 0) {
-		dev_err(gi2c->se.dev, "error turning off device :%d\n", ret);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "error turning off device :%d\n", ret);
 		if (gi2c->xfer_mode == GENI_GPI_DMA)
 			release_gpi_dma(gi2c);
 	}
@@ -2069,13 +2149,14 @@ static int geni_i2c_probe(struct platform_device *pdev)
 	ret = device_property_read_u32(dev, "clock-frequency",
 				       &gi2c->clk_freq_out);
 	if (ret) {
-		i2c_trace_log(dev, "Bus frequency not specified, default to 100kHz.\n");
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, dev,
+			    "Bus frequency not specified, default to 100kHz.\n");
 		gi2c->clk_freq_out = KHZ(100);
 	}
 
 	if (desc && desc->is_ccu_qup) {
 		gi2c->is_ccu_qup = true;
-		i2c_trace_log(&pdev->dev, "%s:CCU QUP enabled\n", __func__);
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, &pdev->dev, "%s:CCU QUP enabled\n", __func__);
 	}
 
 	if (has_acpi_companion(dev))
@@ -2124,13 +2205,13 @@ static int geni_i2c_probe(struct platform_device *pdev)
 
 	if (of_property_read_bool(pdev->dev.of_node, "qcom,shared-se")) {
 		gi2c->is_shared = true;
-		i2c_trace_log(&pdev->dev, "Multi-EE usecase with shared SE\n");
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, &pdev->dev, "Multi-EE usecase with shared SE\n");
 	}
 
 	gi2c->xfer_mode = GENI_SE_INVALID;
 	ret =  geni_i2c_init(gi2c);
 	if (ret < 0) {
-		dev_err(gi2c->se.dev, "geni_i2c_prepare is failed :%d\n", ret);
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev, "geni_i2c_prepare is failed :%d\n", ret);
 		goto err;
 	}
 
@@ -2140,9 +2221,13 @@ static int geni_i2c_probe(struct platform_device *pdev)
 		pm_runtime_disable(gi2c->se.dev);
 		goto err;
 	}
-													
-	i2c_trace_log(dev, "Geni-I2C adaptor successfully added\n");
 
+	ret = device_create_file(gi2c->se.dev, &dev_attr_log_level);
+	if (ret)
+		I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+			    "Failed to create sysfs entry ret:%d\n", ret);
+
+	I2C_DBG_LOG(LOG_LVL_VERBOSE, dev, "Geni-I2C adaptor successfully added\n");
 err:
 	return ret;
 }
@@ -2151,6 +2236,7 @@ static void geni_i2c_remove(struct platform_device *pdev)
 {
 	struct geni_i2c_dev *gi2c = platform_get_drvdata(pdev);
 
+	device_remove_file(gi2c->se.dev, &dev_attr_log_level);
 	i2c_del_adapter(&gi2c->adap);
 	release_gpi_dma(gi2c);
 	pm_runtime_disable(gi2c->se.dev);
@@ -2175,7 +2261,8 @@ static int __maybe_unused geni_i2c_runtime_suspend(struct device *dev)
 	if (gi2c->dev_data->geni_se_switch_state) {
 		ret = gi2c->dev_data->geni_se_switch_state(dev, false);
 		if (ret) {
-			dev_err(dev, "failed to switch resources, ret:%d\n", ret);
+			I2C_ERR_LOG(LOG_LVL_ERROR, dev,
+				    "failed to switch resources, ret:%d\n", ret);
 			return ret;
 		}
 	}
@@ -2192,7 +2279,8 @@ static int __maybe_unused geni_i2c_runtime_resume(struct device *dev)
 	if (gi2c->dev_data->geni_se_switch_state) {
 		ret = gi2c->dev_data->geni_se_switch_state(dev, true);
 		if (ret) {
-			dev_err(dev, "failed to switch resources, ret:%d\n", ret);
+			I2C_ERR_LOG(LOG_LVL_ERROR, dev,
+				    "failed to switch resources, ret:%d\n", ret);
 			return ret;
 		}
 	}
@@ -2216,7 +2304,7 @@ static int __maybe_unused geni_i2c_suspend_noirq(struct device *dev)
 
 	ret = pm_runtime_force_suspend(dev);
 	if (ret) {
-		dev_err(dev, "%s: force suspend failed\n", __func__);
+		I2C_ERR_LOG(LOG_LVL_ERROR, dev, "%s: force suspend failed\n", __func__);
 		i2c_mark_adapter_resumed(&gi2c->adap);
 	}
 	return ret;
@@ -2235,12 +2323,13 @@ static int __maybe_unused geni_i2c_resume_noirq(struct device *dev)
 		return ret;
 
 	if (pm_suspend_target_state == PM_SUSPEND_MEM) {
-		dev_dbg(dev, "%s: In S2R\n", __func__);
+		I2C_DBG_LOG(LOG_LVL_VERBOSE, dev, "%s: In S2R\n", __func__);
 		gi2c->xfer_mode = GENI_SE_INVALID;
 
 		ret = geni_i2c_init(gi2c);
 		if (ret) {
-			dev_err(gi2c->se.dev, "geni_i2c_prepare is failed :%d\n", ret);
+			I2C_ERR_LOG(LOG_LVL_ERROR, gi2c->se.dev,
+				    "geni_i2c_prepare is failed:%d\n", ret);
 			return ret;
 		}
 	}
diff --git a/drivers/soc/qcom/qcom_ccu_qup.c b/drivers/soc/qcom/qcom_ccu_qup.c
index 17c31b773f22..b1f535009eb9 100644
--- a/drivers/soc/qcom/qcom_ccu_qup.c
+++ b/drivers/soc/qcom/qcom_ccu_qup.c
@@ -26,6 +26,38 @@
 #include <linux/uaccess.h>
 #include <linux/wait.h>
 
+#define CREATE_TRACE_POINTS
+#include <trace/events/ccu_qup_trace.h>
+
+enum DEBUG_LOG_LVL {
+	LOG_LVL_ERROR,
+	LOG_LVL_INFO,
+	LOG_LVL_VERBOSE,
+	LOG_LVL_MAX,
+};
+
+void ccu_trace_log(struct device *dev, const char *fmt, ...)
+{
+	va_list args;
+	struct va_format vaf;
+
+	va_start(args, fmt);
+	vaf.fmt = fmt;
+	vaf.va = &args;
+
+	trace_ccu_log_info(dev_name(dev), &vaf);
+}
+
+#define CCU_LOG(log_fn, level, dev, fmt, ...) do { \
+	if ((level) <= log_level) \
+		ccu_trace_log(dev, fmt, ##__VA_ARGS__); \
+	else \
+		log_fn(dev, fmt, ##__VA_ARGS__); \
+} while (0)
+
+#define CCU_ERR_LOG(level, dev, fmt, ...) CCU_LOG(dev_err, level, dev, fmt, ##__VA_ARGS__)
+#define CCU_DBG_LOG(level, dev, fmt, ...) CCU_LOG(dev_dbg, level, dev, fmt, ##__VA_ARGS__)
+
 #define GPII_0_IRQ		0
 #define GPII_14_IRQ		19
 #define MAX_GPI_DEV		2
@@ -37,6 +69,27 @@
 
 struct ccu_qup *ccud;
 
+static int log_level = -1;
+static ssize_t log_level_show(struct device *dev,
+			      struct device_attribute *attr, char *buf)
+{
+	return snprintf(buf, PAGE_SIZE, "%d\n", log_level);
+}
+
+static ssize_t log_level_store(struct device *dev,
+			       struct device_attribute *attr,
+			       const char *buf, size_t count)
+{
+	int val;
+
+	if (kstrtoint(buf, 10, &val) != 0 || val < LOG_LVL_ERROR || val >= LOG_LVL_MAX)
+		return -EINVAL;
+
+	log_level = val;
+	return count;
+}
+static DEVICE_ATTR_RW(log_level);
+
 static power_on_off_t ccu_on_off;
 static recover_ios_line_t ccu_recover;
 static config_ccu_mode_t ccu_cfg_mode;
@@ -101,7 +154,7 @@ static ssize_t ccu_response_read(struct file *filp, char __user *buf,
 
 	ccu = filp->private_data;
 	if (list_empty(&ccu->response_list)) {
-		dev_err(ccu->dev, "No rx response data avilable\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "No rx response data available\n");
 		return -EAGAIN;
 	}
 
@@ -110,7 +163,7 @@ static ssize_t ccu_response_read(struct file *filp, char __user *buf,
 		ret = copy_to_user(buf, entry->res_buf,
 				   sizeof(struct ccu_xfer_result));
 		if (ret) {
-			dev_err(ccu->dev, "copy to user failed\n");
+			CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "copy to user failed\n");
 			return -EAGAIN;
 		}
 		list_del(&entry->list);
@@ -118,6 +171,8 @@ static ssize_t ccu_response_read(struct file *filp, char __user *buf,
 		kfree(entry);
 		atomic_dec(&ccu->rx_avail);
 
+		CCU_DBG_LOG(LOG_LVL_VERBOSE, ccu->dev, "%s: Read operation done for %d byte\n",
+			    __func__, sizeof(struct ccu_xfer_result));
 		return sizeof(struct ccu_xfer_result);
 	}
 
@@ -142,7 +197,7 @@ static __poll_t ccu_poll(struct file *filp, poll_table *wait)
 	ccu = filp->private_data;
 	poll_wait(filp, &ccu->readq, wait);
 	if (atomic_read(&ccu->rx_avail)) {
-		dev_dbg(ccu->dev, "Response avilable to read\n");
+		CCU_DBG_LOG(LOG_LVL_VERBOSE, ccu->dev, "Response available to read\n");
 		return (POLLIN | POLLRDNORM);
 	}
 
@@ -164,14 +219,14 @@ void ccu_update_xfer_result(struct ccu_xfer_result *result)
 	mutex_lock(&ccu->lock);
 	entry = kmalloc(sizeof(*entry), GFP_KERNEL);
 	if (!entry) {
-		dev_err(ccu->dev, "Memory allocation failed for entry\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "Memory allocation failed for entry\n");
 		mutex_unlock(&ccu->lock);
 		return;
 	}
 
 	entry->res_buf = kmalloc(sizeof(*result), GFP_KERNEL);
 	if (!entry->res_buf) {
-		dev_err(ccu->dev, "Memory allocation failed for buf\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "Memory allocation failed for buf\n");
 		kfree(entry);
 		mutex_unlock(&ccu->lock);
 		return;
@@ -196,6 +251,7 @@ static int ccu_release(struct inode *inode, struct file *filp)
 
 	ccu = filp->private_data;
 
+	CCU_DBG_LOG(LOG_LVL_VERBOSE, ccu->dev, "Release done\n");
 	return 0;
 }
 
@@ -207,12 +263,13 @@ static int ccu_open(struct inode *inode, struct file *filp)
 	cdev = inode->i_cdev;
 	ccu = container_of(cdev, struct ccu_qup, cdev);
 	if (!ccu) {
-		dev_err(ccu->dev, "Err to get the CCU\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "Err to get the CCU\n");
 		return -ENODEV;
 	}
 
 	filp->private_data = ccu;
 
+	CCU_DBG_LOG(LOG_LVL_VERBOSE, ccu->dev, "Open done\n");
 	return 0;
 }
 
@@ -225,7 +282,8 @@ int ccu_power_on_off(struct i2c_adapter *adap, bool suspend)
 
 	ret = ccu_on_off(adap, suspend);
 	if (ret) {
-		pr_err("failed to turn on/off the clocks:%d\n", suspend);
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccud->dev,
+			    "failed to turn on/off the clocks:%d\n", suspend);
 		return -EAGAIN;
 	}
 
@@ -335,13 +393,15 @@ int ccu_sync_id_to_sync_ch_mapping(struct ccu_qup *ccu, struct sync_info sync[])
 		if ((sync[i].qup_no > CCU_MAX_QUP) ||
 				(sync[i].se_no > CCU_MAX_SE) ||
 				(sync[i].sync_id > CCU_SYNC_TIMERS)) {
-			dev_err(ccu->dev,
-				"%s: Invalid request: QUP:%d SE:%d ID:%d\n",
-				__func__, sync[i].qup_no, sync[i].se_no,
-				sync[i].sync_id);
+			CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev,
+				    "%s: Invalid request: QUP:%d SE:%d ID:%d\n", __func__,
+				    sync[i].qup_no, sync[i].se_no, sync[i].sync_id);
 			return -EINVAL;
 		}
 
+		CCU_DBG_LOG(LOG_LVL_VERBOSE, ccud->dev, "%s: QUP:%d SE:%d ID:%d, ch:%d\n",
+			    __func__, sync[i].qup_no, sync[i].se_no,
+			    sync[i].sync_id, sync[i].sync_ch);
 		qup_idx = sync[i].qup_no ? 1: 0;
 		if ((sync[i].sync_id % 4) == 0)
 			offset = 0;
@@ -362,7 +422,8 @@ int ccu_sync_id_to_sync_ch_mapping(struct ccu_qup *ccu, struct sync_info sync[])
 			val = (1 << (sync[i].se_no + offset));
 			ccu_update_ch_id_mapping(ccu, qup_idx, i, val, true);
 		} else {
-			dev_err(ccu->dev, "%s Invalid sync ch:%d\n", __func__, sync[i].sync_ch);
+			CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev,
+				    "%s Invalid sync ch:%d\n", __func__, sync[i].sync_ch);
 			return -EINVAL;
 		}
 	}
@@ -388,12 +449,14 @@ int ccu_configure_mode(struct i2c_adapter *adap, struct ccu_qup_config *cfg)
 	ret = ccu_cfg_mode(adap, cfg->gpii_no,
 					  cfg->ch_type, cfg->ccu_mode);
 	if (ret) {
-		dev_err(ccud->dev, "Configuration of CCU Mode is failed for gpii-%d of i2c-%d\n",
-				cfg->gpii_no, adap->nr);
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccud->dev,
+			    "Configuration of CCU Mode is failed for gpii-%d of i2c-%d\n",
+			    cfg->gpii_no, adap->nr);
 		return ret;
 	}
-	dev_dbg(ccud->dev, "Configuration of CCU Mode is done for gpii-%d\n of i2c-%d\n",
-			cfg->gpii_no, adap->nr);
+	CCU_DBG_LOG(LOG_LVL_INFO, ccud->dev,
+		    "Configuration of CCU Mode is done for gpii-%d\n of i2c-%d\n",
+		    cfg->gpii_no, adap->nr);
 	return 0;
 }
 
@@ -407,20 +470,20 @@ int ccu_qup_load_firmware(void)
 	int ret = 0;
 
 	if (ccud->fw_loaded) {
-		dev_err(ccud->dev, "Fw is already loaded it\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccud->dev, "Fw is already loaded it\n");
 		return 0;
 	}
 
 	/* Make SCM call, to load the TZ FW */
 	ret = qcom_scm_load_ccu_qup_fw(SCM_LOAD_QUP_TYPE);
 	if (ret) {
-		dev_err(ccud->dev, "Unable to load the FW\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccud->dev, "Unable to load the FW\n");
 		ccud->fw_loaded = false;
 		return ret;
 	}
 	ccud->fw_loaded = true;
 
-	dev_dbg(ccud->dev, "CCU QUP FW Load done successfully\n");
+	CCU_DBG_LOG(LOG_LVL_INFO, ccud->dev, "CCU QUP FW Load done successfully\n");
 	return ret;
 }
 EXPORT_SYMBOL(ccu_qup_load_firmware);
@@ -442,6 +505,8 @@ int ccu_gpii_reset(struct i2c_adapter *adap, int gpii_no, bool partial,
 	if (!adap || (gpii_no > CCU_MAX_GPII))
 		return -EINVAL;
 
+	CCU_DBG_LOG(LOG_LVL_INFO, ccud->dev, "%s: gpii:%d, paritial:%d, premature:%d\n",
+		    __func__, gpii_no, partial, premature_cancel);
 	if (partial) {
 		ret = ccu_reset(adap, gpii_no, premature_cancel);
 		if (ret)
@@ -513,13 +578,13 @@ static ssize_t ccu_config_write(struct file *filp, const char __user *buf,
 
 	ccu = filp->private_data;
 	if (len != sizeof(struct ccu_qup_config)) {
-		dev_err(ccu->dev, "Invalid data size\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "Invalid data size\n");
 		return -EINVAL;
 	}
 
 	ccu->cfg = kmalloc (sizeof(struct ccu_qup_config), GFP_KERNEL);
 	if (!ccu->cfg) {
-		dev_err(ccu->dev, "failed to allocate memory\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "failed to allocate memory\n");
 		return -ENOMEM;
 	}
 
@@ -534,12 +599,13 @@ static ssize_t ccu_config_write(struct file *filp, const char __user *buf,
 
 	ret = ccu_get_se_adapter(cfg.fd, ccu);
 	if (ret) {
-		dev_err(ccu->dev, "failed to get SE Adapter\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "failed to get SE Adapter\n");
 		kfree(ccu->cfg);
 		ccu->cfg = NULL;
 		return -EINVAL;
 	}
 
+	CCU_DBG_LOG(LOG_LVL_INFO, ccud->dev, "%s: CMD:%d\n", __func__, ccu->cfg->cmd);
 	switch (ccu->cfg->cmd) {
 	case I2C_GPII_RESET:
 		ret = ccu_gpii_reset(ccu->adap, ccu->cfg->gpii_no, ccu->cfg->rst_flag,
@@ -567,7 +633,7 @@ static ssize_t ccu_config_write(struct file *filp, const char __user *buf,
 		ret = ccu_power_on_off(ccu->adap, true);
 		break;
 	default:
-		dev_err(ccu->dev, "Invalid cmd:%d\n", ccu->cfg->cmd);
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "Invalid cmd:%d\n", ccu->cfg->cmd);
 		break;
 	}
 	kfree(ccu->cfg);
@@ -602,8 +668,9 @@ static irqreturn_t gpi0_irq_thread(int irq, void *dev_id)
 			}
 		} else {
 			if (irq_stat & (1U << i)) {
-				dev_dbg(ccu->dev, "IRQ for [CCU QUP0]-GPII[%d] IRQ_STATUS:%d\n",
-						(i- 5), irq_stat);
+				CCU_DBG_LOG(LOG_LVL_VERBOSE, ccu->dev,
+					    "IRQ for [CCU QUP0]-GPII[%d] IRQ_STATUS:%d\n",
+					    (i - 5), irq_stat);
 				gpi_handle_irq(ccu->gpi_pdev[0], (i - 5));
 			}
 		}
@@ -642,8 +709,9 @@ static irqreturn_t gpi1_irq_thread(int irq, void *dev_id)
 			}
 		} else {
 			if (irq_stat & (1U << i)) {
-				dev_dbg(ccu->dev, "IRQ for [CCU QUP1]-GPII[%d] IRQ_STATUS:%d\n",
-						(i- 5), irq_stat);
+				CCU_DBG_LOG(LOG_LVL_VERBOSE, ccu->dev,
+					    "IRQ for [CCU QUP1]-GPII[%d] IRQ_STATUS:%d\n",
+					    (i - 5), irq_stat);
 				gpi_handle_irq(ccu->gpi_pdev[1], (i - 5));
 			}
 		}
@@ -716,14 +784,16 @@ static void get_set_child_dev_node(struct work_struct *work)
 	for (i = 0; i < MAX_GPI_DEV; i++) {
 		gpi_node = of_get_child_by_name(node, gpi[i]);
 		if (!gpi_node) {
-			dev_err(ccu->dev, "failed to find gpi_dma[%d] node\n", i);
+			CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev,
+				    "failed to find gpi_dma[%d] node\n", i);
 			kfree(child_status);
 			return;
 		}
 		/* Find the platform_device associated with a node */
 		ccu->gpi_pdev[i] = of_find_device_by_node(gpi_node);
 		if (!ccu->gpi_pdev[i]) {
-			dev_err(ccu->dev, "failed to find platform device for gpi_dma[%d]\n", i);
+			CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev,
+				    "failed to find platform device for gpi_dma[%d]\n", i);
 			of_node_put(gpi_node);
 			kfree(child_status);
 			return;
@@ -758,7 +828,7 @@ static int ccu_get_configure_irq(struct platform_device *pdev, struct ccu_qup *c
 	ret = devm_request_threaded_irq(ccu->dev, ccu->ccu_qup0_irq, ccu0_irq_handler,
 					gpi0_irq_thread, 0, "CCU_QUP0", ccu);
 	if (ret) {
-		dev_err(ccu->dev, "Request IRQ failed\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "Request IRQ failed\n");
 		return -EINVAL;
 	}
 
@@ -766,7 +836,7 @@ static int ccu_get_configure_irq(struct platform_device *pdev, struct ccu_qup *c
 	ret = devm_request_threaded_irq(ccu->dev, ccu->ccu_qup1_irq, ccu1_irq_handler,
 					gpi1_irq_thread, 0, "CCU_QUP1", ccu);
 	if (ret) {
-		dev_err(ccu->dev, "Request IRQ failed\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "Request IRQ failed\n");
 		return -EINVAL;
 	}
 
@@ -774,6 +844,7 @@ static int ccu_get_configure_irq(struct platform_device *pdev, struct ccu_qup *c
 	writel_relaxed(enable, (ccu->qup_irq_base[0] + CCU_QUP_IRQ_MASK));
 	writel_relaxed(enable, (ccu->qup_irq_base[1] + CCU_QUP_IRQ_MASK));
 
+	CCU_DBG_LOG(LOG_LVL_VERBOSE, ccu->dev, "IRQ Configuration done\n");
 	return 0;
 }
 
@@ -790,30 +861,32 @@ static int ccu_qup_create_chrdev_node(struct ccu_qup *ccu)
 
 	ret = alloc_chrdev_region(&ccu->ccu_dev, 0, 1, "ccu_dev");
 	if (ret < 0) {
-		dev_err(ccu->dev, "failed to allocate chrdev region:%d\n", ret);
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "failed to allocate chrdev region:%d\n", ret);
 		return ret;
 	}
 
 	cdev_init(&ccu->cdev, &ccu_fops);
 	ret = cdev_add(&ccu->cdev, ccu->ccu_dev, 1);
 	if (ret < 0) {
-		dev_err(ccu->dev, "failed to add cdev%d\n", ret);
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "failed to add cdev%d\n", ret);
 		goto err_chrdev_region;
 	}
 
 	ccu->ccu_class = class_create("ccu_class");
 	if (IS_ERR(ccu->ccu_class)) {
 		ret = PTR_ERR(ccu->ccu_class);
-		dev_err(ccu->dev, "failed to create struct class\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "failed to create struct class\n");
 		goto err_cdev_del;
 	}
 
 	ccu->class_dev = device_create(ccu->ccu_class, NULL, ccu->ccu_dev, NULL, "ccu_device");
 	if (IS_ERR(ccu->class_dev)) {
 		ret = PTR_ERR(ccu->class_dev);
-		dev_err(ccu->dev, "failed to create device\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, ccu->dev, "failed to create device\n");
 		goto err_class_create;
 	}
+
+	CCU_DBG_LOG(LOG_LVL_VERBOSE, ccu->dev, "Char dev created\n");
 	return 0;
 err_class_create:
 	class_destroy(ccu->ccu_class);
@@ -838,49 +911,49 @@ static int ccu_qup_probe(struct platform_device *pdev)
 	ccu->dev = &pdev->dev;
 	res = platform_get_resource_byname(pdev, IORESOURCE_MEM, "ccu_qupv3_0_common");
 	if (!res) {
-		dev_err(&pdev->dev, "missing 'reg' for CCU0 common base\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, &pdev->dev, "missing 'reg' for CCU0 common base\n");
 		return -EINVAL;
 	}
 
 	ccu->qup0_common_reg = devm_ioremap(&pdev->dev, res->start, resource_size(res));
 	if (!ccu->qup0_common_reg) {
-		dev_err(&pdev->dev, "ioremap failed for qup0_common_reg\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, &pdev->dev, "ioremap failed for qup0_common_reg\n");
 		return -ENODEV;
 	}
 
 	res = platform_get_resource_byname(pdev, IORESOURCE_MEM, "ccu_qupv3_1_common");
 	if (!res) {
-		dev_err(&pdev->dev, "missing 'reg' for CCU1 common base\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, &pdev->dev, "missing 'reg' for CCU1 common base\n");
 		return -EINVAL;
 	}
 
 	ccu->qup1_common_reg = devm_ioremap(&pdev->dev, res->start, resource_size(res));
 	if (!ccu->qup1_common_reg) {
-		dev_err(&pdev->dev, "ioremap failed for qup1_common_reg\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, &pdev->dev, "ioremap failed for qup1_common_reg\n");
 		return -ENODEV;
 	}
 
 	res = platform_get_resource_byname(pdev, IORESOURCE_MEM, "ccu_qupv3_0_IRQ");
 	if (!res) {
-		dev_err(&pdev->dev, "missing 'reg' for CCU0 IRQ\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, &pdev->dev, "missing 'reg' for CCU0 IRQ\n");
 		return -EINVAL;
 	}
 
 	ccu->qup_irq_base[0] = devm_ioremap(&pdev->dev, res->start, resource_size(res));
 	if (!ccu->qup_irq_base[0]) {
-		dev_err(&pdev->dev, "ioremap failed for qup_irq_base[0]\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, &pdev->dev, "ioremap failed for qup_irq_base[0]\n");
 		return -ENODEV;
 	}
 
 	res = platform_get_resource_byname(pdev, IORESOURCE_MEM, "ccu_qupv3_1_IRQ");
 	if (!res) {
-		dev_err(&pdev->dev, "missing 'reg' for CCU1 IRQ\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, &pdev->dev, "missing 'reg' for CCU1 IRQ\n");
 		return -EINVAL;
 	}
 
 	ccu->qup_irq_base[1] = devm_ioremap(&pdev->dev, res->start, resource_size(res));
 	if (!ccu->qup_irq_base[1]) {
-		dev_err(&pdev->dev, "ioremap failed for qup_irq_base[1]\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, &pdev->dev, "ioremap failed for qup_irq_base[1]\n");
 		return -ENODEV;
 	}
 
@@ -900,7 +973,7 @@ static int ccu_qup_probe(struct platform_device *pdev)
 
 	ret = ccu_get_configure_irq(pdev, ccu);
 	if (ret) {
-		dev_err(&pdev->dev, "IRQ configuration failed for CCU\n");
+		CCU_ERR_LOG(LOG_LVL_ERROR, &pdev->dev, "IRQ configuration failed for CCU\n");
 		return ret;
 	}
 
@@ -914,7 +987,12 @@ static int ccu_qup_probe(struct platform_device *pdev)
 	queue_delayed_work(ccu->gpi_wq, &ccu->get_child_dev_node,
 			   msecs_to_jiffies(3000));
 
-	dev_dbg(&pdev->dev, "CCU QUP Probe completed\n");
+	ret = device_create_file(&pdev->dev, &dev_attr_log_level);
+	if (ret)
+		CCU_ERR_LOG(LOG_LVL_ERROR, &pdev->dev,
+			    "Failed to create sysfs entry ret:%d\n", ret);
+
+	CCU_DBG_LOG(LOG_LVL_INFO, &pdev->dev, "CCU QUP Probe completed\n");
 
 	return devm_of_platform_populate(&pdev->dev);
 }
@@ -926,7 +1004,7 @@ static int ccu_qup_remove(struct platform_device *pdev)
 	if (!ccu)
 		return -ENODEV;
 
-	/* Disable the CCU QUP0 & CCU QUP1 IRQ */
+	device_remove_file(&pdev->dev, &dev_attr_log_level);
 	disable_irq(ccu->ccu_qup0_irq);
 	disable_irq(ccu->ccu_qup1_irq);
 	destroy_workqueue(ccu->gpi_wq);
@@ -935,7 +1013,7 @@ static int ccu_qup_remove(struct platform_device *pdev)
 	cdev_del(&ccu->cdev);
 	unregister_chrdev_region(ccu->ccu_dev, 1);
 
-	dev_dbg(ccu->dev, "CCU QUP Remove done\n");
+	CCU_DBG_LOG(LOG_LVL_INFO, ccu->dev, "CCU QUP Remove done\n");
 	return 0;
 }
 
diff --git a/drivers/spi/spi-geni-qcom.c b/drivers/spi/spi-geni-qcom.c
index b769adb43073..578d7739fe7f 100644
--- a/drivers/spi/spi-geni-qcom.c
+++ b/drivers/spi/spi-geni-qcom.c
@@ -80,7 +80,7 @@
 #define MAX_NUM_SE_CLK_FREQ	6
 
 #define CREATE_TRACE_POINTS
-#include <trace/events/qup_buses_trace.h>
+#include <trace/events/qup_spi_trace.h>
 
 void spi_trace_log(struct device *dev, const char *fmt, ...)
 {
@@ -92,7 +92,7 @@ void spi_trace_log(struct device *dev, const char *fmt, ...)
 
 	va_start(args, fmt);
 	vaf.va = &args;
-	trace_buses_log_info(dev_name(dev), &vaf);
+	trace_spi_log_info(dev_name(dev), &vaf);
 	va_end(args);
 }
 
diff --git a/drivers/tty/serial/qcom_geni_serial.c b/drivers/tty/serial/qcom_geni_serial.c
index 238a6f167c53..805ec9ff4bf0 100644
--- a/drivers/tty/serial/qcom_geni_serial.c
+++ b/drivers/tty/serial/qcom_geni_serial.c
@@ -28,7 +28,7 @@
 #include <dt-bindings/interconnect/qcom,icc.h>
 
 #define CREATE_TRACE_POINTS
-#include <trace/events/qup_buses_trace.h>
+#include <trace/events/qup_serial_trace.h>
 
 void serial_trace_log(struct device *dev, const char *fmt, ...)
 {
@@ -40,7 +40,7 @@ void serial_trace_log(struct device *dev, const char *fmt, ...)
 
 	va_start(args, fmt);
 	vaf.va = &args;
-	trace_buses_log_info(dev_name(dev), &vaf);
+	trace_serial_log_info(dev_name(dev), &vaf);
 	va_end(args);
 }
 
diff --git a/include/trace/events/ccu_qup_trace.h b/include/trace/events/ccu_qup_trace.h
new file mode 100644
index 000000000000..2dc3048feb6d
--- /dev/null
+++ b/include/trace/events/ccu_qup_trace.h
@@ -0,0 +1,15 @@
+/* SPDX-License-Identifier: GPL-2.0
+ * Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
+ */
+#undef TRACE_SYSTEM
+#define TRACE_SYSTEM ccu_qup_trace
+
+#if !defined(_TRACE_CCU_QUP_TRACE_H) || defined(TRACE_HEADER_MULTI_READ)
+#define _TRACE_CCU_QUP_TRACE_H
+
+#include "qup_trace_common.h"
+
+DEFINE_QUP_LOG_EVENT(ccu_log_info);
+
+#endif /* _TRACE_CCU_QUP_TRACE_H */
+#include <trace/define_trace.h>
diff --git a/include/trace/events/gpi_qup_trace.h b/include/trace/events/gpi_qup_trace.h
new file mode 100644
index 000000000000..d7b95eab3847
--- /dev/null
+++ b/include/trace/events/gpi_qup_trace.h
@@ -0,0 +1,16 @@
+/* SPDX-License-Identifier: GPL-2.0
+ * Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
+ */
+#undef TRACE_SYSTEM
+#define TRACE_SYSTEM gpi_qup_trace
+
+#if !defined(_TRACE_QUP_GPI_TRACE_H) || defined(TRACE_HEADER_MULTI_READ)
+#define _TRACE_QUP_GPI_TRACE_H
+
+#include "qup_trace_common.h"
+
+DEFINE_QUP_LOG_EVENT(gpi_log_info);
+
+#endif /* _TRACE_QUP_GPI_TRACE_H */
+#include <trace/define_trace.h>
+
diff --git a/include/trace/events/qup_buses_trace.h b/include/trace/events/qup_buses_trace.h
deleted file mode 100644
index 944abaa86d95..000000000000
--- a/include/trace/events/qup_buses_trace.h
+++ /dev/null
@@ -1,113 +0,0 @@
-/* SPDX-License-Identifier: GPL-2.0
- * Copyright (c) 2023-2024 Qualcomm Innovation Center, Inc. All rights reserved.
- */
-
-#undef TRACE_SYSTEM
-#define TRACE_SYSTEM qup_buses_trace
-
-#if !defined(_TRACE_QUP_BUSES_TRACE_H) || defined(TRACE_HEADER_MULTI_READ)
-#define _TRACE_QUP_BUSES_TRACE_H
-
-#include <linux/ktime.h>
-#include <linux/tracepoint.h>
-
-#define MAX_MSG_LEN 256
-
-TRACE_EVENT(buses_log_info,
-
-	TP_PROTO(const char *name, struct va_format *vaf),
-
-	TP_ARGS(name, vaf),
-
-	TP_STRUCT__entry(
-		__string(name, name)
-		__dynamic_array(char, msg, MAX_MSG_LEN)
-	),
-
-	TP_fast_assign(
-		__assign_str_len(name, name, strlen(name));
-		WARN_ON_ONCE(vsnprintf(__get_dynamic_array(msg),
-				       MAX_MSG_LEN, vaf->fmt,
-				       *vaf->va) >= MAX_MSG_LEN);
-	),
-
-	TP_printk("%s: %s", __get_str(name), __get_str(msg))
-);
-
-DECLARE_EVENT_CLASS(buses_info,
-
-	TP_PROTO(struct device *dev, const char *string1, char *string2),
-
-	TP_ARGS(dev, string1, string2),
-
-	TP_STRUCT__entry(
-		__string(name, dev_name(dev))
-		__string(string1, string1)
-		__string(string2, string2)
-	),
-
-	TP_fast_assign(
-		__assign_str_len(name, dev_name(dev), strlen(dev_name(dev)));
-		__assign_str_len(string1, string1, strlen(string1));
-		__assign_str_len(string2, string2, strlen(string2));
-	),
-
-	TP_printk("%s: %s: %s", __get_str(name), __get_str(string1), __get_str(string2))
-);
-
-DECLARE_EVENT_CLASS(serial_transmit_data,
-
-	TP_PROTO(struct device *dev, char *string, int size),
-
-	TP_ARGS(dev, string, size),
-
-	TP_STRUCT__entry(
-		__string(name, dev_name(dev))
-		__array(char, buf, 64)
-		__field(unsigned int, size)
-		__field(int, len)
-	),
-
-	TP_fast_assign(
-		__assign_str_len(name, dev_name(dev), strlen(dev_name(dev)));
-		__entry->len = min(32, size);
-		hex_dump_to_buffer(string, __entry->len, 32, 1, __entry->buf,
-				   sizeof(__entry->buf), false);
-	),
-
-	TP_printk("%s: %s\n", __get_str(name),  __entry->buf)
-);
-
-
-DEFINE_EVENT(buses_info, serial_info,
-
-	TP_PROTO(struct device *dev, const char *string1, char *string2),
-
-	TP_ARGS(dev, string1, string2)
-);
-
-DEFINE_EVENT(serial_transmit_data, serial_transmit_data_tx,
-
-	TP_PROTO(struct device *dev, char *string, int size),
-
-	TP_ARGS(dev, string, size)
-);
-
-DEFINE_EVENT(serial_transmit_data, serial_transmit_data_rx,
-
-	TP_PROTO(struct device *dev, char *string, int size),
-
-	TP_ARGS(dev, string, size)
-);
-
-DEFINE_EVENT(buses_info, spi_info,
-
-	TP_PROTO(struct device *dev, const char *string1, char *string2),
-
-	TP_ARGS(dev, string1, string2)
-);
-
-#endif /* _TRACE_QUP_BUSES_TRACE_H */
-
-/* This part must be outside protection */
-#include <trace/define_trace.h>
diff --git a/include/trace/events/qup_i2c_trace.h b/include/trace/events/qup_i2c_trace.h
new file mode 100644
index 000000000000..f19b2cd4ce11
--- /dev/null
+++ b/include/trace/events/qup_i2c_trace.h
@@ -0,0 +1,15 @@
+/* SPDX-License-Identifier: GPL-2.0
+ * Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
+ */
+#undef TRACE_SYSTEM
+#define TRACE_SYSTEM qup_i2c_trace
+
+#if !defined(_TRACE_QUP_I2C_TRACE_H) || defined(TRACE_HEADER_MULTI_READ)
+#define _TRACE_QUP_I2C_TRACE_H
+
+#include "qup_trace_common.h"
+
+DEFINE_QUP_LOG_EVENT(i2c_log_info);
+
+#endif /* _TRACE_QUP_I2C_TRACE_H */
+#include <trace/define_trace.h>
diff --git a/include/trace/events/qup_serial_trace.h b/include/trace/events/qup_serial_trace.h
new file mode 100644
index 000000000000..0367617df155
--- /dev/null
+++ b/include/trace/events/qup_serial_trace.h
@@ -0,0 +1,31 @@
+/* SPDX-License-Identifier: GPL-2.0
+ * Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
+ */
+#undef TRACE_SYSTEM
+#define TRACE_SYSTEM qup_serial_trace
+
+#if !defined(_TRACE_QUP_SERIAL_TRACE_H) || defined(TRACE_HEADER_MULTI_READ)
+#define _TRACE_QUP_SERIAL_TRACE_H
+
+#include "qup_trace_common.h"
+
+DEFINE_QUP_LOG_EVENT(serial_log_info);
+
+DECLARE_QUP_INFO_EVENT_CLASS(serial_info_event);
+DEFINE_EVENT(serial_info_event, serial_info,
+	     TP_PROTO(struct device *dev, const char *string1, char *string2),
+	     TP_ARGS(dev, string1, string2)
+);
+
+DECLARE_QUP_DATA_EVENT_CLASS(serial_transmit_data);
+DEFINE_EVENT(serial_transmit_data, serial_transmit_data_tx,
+	     TP_PROTO(struct device *dev, char *string, int size),
+	     TP_ARGS(dev, string, size)
+);
+DEFINE_EVENT(serial_transmit_data, serial_transmit_data_rx,
+	     TP_PROTO(struct device *dev, char *string, int size),
+	     TP_ARGS(dev, string, size)
+);
+
+#endif /* _TRACE_QUP_SERIAL_TRACE_H */
+#include <trace/define_trace.h>
diff --git a/include/trace/events/qup_spi_trace.h b/include/trace/events/qup_spi_trace.h
new file mode 100644
index 000000000000..e50b19655c99
--- /dev/null
+++ b/include/trace/events/qup_spi_trace.h
@@ -0,0 +1,21 @@
+/* SPDX-License-Identifier: GPL-2.0
+ * Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
+ */
+#undef TRACE_SYSTEM
+#define TRACE_SYSTEM qup_spi_trace
+
+#if !defined(_TRACE_QUP_SPI_TRACE_H) || defined(TRACE_HEADER_MULTI_READ)
+#define _TRACE_QUP_SPI_TRACE_H
+
+#include "qup_trace_common.h"
+
+DEFINE_QUP_LOG_EVENT(spi_log_info);
+
+DECLARE_QUP_INFO_EVENT_CLASS(spi_info_event);
+DEFINE_EVENT(spi_info_event, spi_info,
+	     TP_PROTO(struct device *dev, const char *string1, char *string2),
+	     TP_ARGS(dev, string1, string2)
+);
+
+#endif /* _TRACE_QUP_SPI_TRACE_H */
+#include <trace/define_trace.h>
diff --git a/include/trace/events/qup_trace_common.h b/include/trace/events/qup_trace_common.h
new file mode 100644
index 000000000000..66e9d04a8084
--- /dev/null
+++ b/include/trace/events/qup_trace_common.h
@@ -0,0 +1,65 @@
+/* SPDX-License-Identifier: GPL-2.0
+ * Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
+ */
+#ifndef _TRACE_QUP_COMMON_H
+#define _TRACE_QUP_COMMON_H
+
+#include <linux/ktime.h>
+#include <linux/tracepoint.h>
+
+#define MAX_MSG_LEN 256
+
+#define DEFINE_QUP_LOG_EVENT(event_name) \
+TRACE_EVENT(event_name, \
+	TP_PROTO(const char *name, struct va_format *vaf), \
+	TP_ARGS(name, vaf), \
+	TP_STRUCT__entry( \
+		__string(name, name) \
+		__dynamic_array(char, msg, MAX_MSG_LEN) \
+	), \
+	TP_fast_assign( \
+		__assign_str_len(name, name, strlen(name)); \
+		WARN_ON_ONCE(vsnprintf(__get_dynamic_array(msg), \
+			     MAX_MSG_LEN, vaf->fmt, \
+			     *vaf->va) >= MAX_MSG_LEN); \
+	), \
+	TP_printk("%s: %s", __get_str(name), __get_str(msg)) \
+)
+
+#define DECLARE_QUP_INFO_EVENT_CLASS(class_name) \
+DECLARE_EVENT_CLASS(class_name, \
+	TP_PROTO(struct device *dev, const char *string1, char *string2), \
+	TP_ARGS(dev, string1, string2), \
+	TP_STRUCT__entry( \
+		__string(name, dev_name(dev)) \
+		__string(string1, string1) \
+		__string(string2, string2) \
+	), \
+	TP_fast_assign( \
+		__assign_str_len(name, dev_name(dev), strlen(dev_name(dev))); \
+		__assign_str_len(string1, string1, strlen(string1)); \
+		__assign_str_len(string2, string2, strlen(string2)); \
+	), \
+	TP_printk("%s: %s: %s", __get_str(name), __get_str(string1), __get_str(string2)) \
+)
+
+#define DECLARE_QUP_DATA_EVENT_CLASS(class_name) \
+DECLARE_EVENT_CLASS(class_name, \
+	TP_PROTO(struct device *dev, char *string, int size), \
+	TP_ARGS(dev, string, size), \
+	TP_STRUCT__entry( \
+		__string(name, dev_name(dev)) \
+		__array(char, buf, 64) \
+		__field(unsigned int, size) \
+		__field(int, len) \
+	), \
+	TP_fast_assign( \
+		__assign_str_len(name, dev_name(dev), strlen(dev_name(dev))); \
+		__entry->len = min(32, size); \
+		hex_dump_to_buffer(string, __entry->len, 32, 1, __entry->buf, \
+				   sizeof(__entry->buf), false); \
+	), \
+	TP_printk("%s: %s", __get_str(name), __entry->buf) \
+)
+
+#endif /* _TRACE_QUP_COMMON_H */
-- 
2.34.1

