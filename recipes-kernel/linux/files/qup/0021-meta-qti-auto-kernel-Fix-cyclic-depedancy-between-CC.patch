From af8957290beec330545e3c74b11beff140767d8d Mon Sep 17 00:00:00 2001
From: Aniket Randive <quic_arandive@quicinc.com>
Date: Wed, 9 Apr 2025 10:31:03 +0530
Subject: [PATCH] i2c: Fix circular dependency between I2C and CCU driver

Remove export symbol functions and use function pointers
to resolve circular dependency between I2C and CCU drivers.

Change-Id: Ieb153f07f439692994f500a7978845da3085845a
Signed-off-by: Aniket Randive <quic_arandive@quicinc.com>
---
 drivers/dma/qcom/gpi.c                |  40 ++++---
 drivers/i2c/busses/i2c-qcom-geni.c    | 160 ++++++++++++++++++--------
 drivers/soc/qcom/qcom_ccu_qup.c       |  98 ++++++++++++----
 include/linux/dma/qcom-gpi-dma.h      |   2 -
 include/linux/soc/qcom/qcom_ccu_qup.h |  26 +++--
 5 files changed, 232 insertions(+), 94 deletions(-)

diff --git a/drivers/dma/qcom/gpi.c b/drivers/dma/qcom/gpi.c
index 0203c00f9f85..4817fdebde15 100644
--- a/drivers/dma/qcom/gpi.c
+++ b/drivers/dma/qcom/gpi.c
@@ -15,6 +15,7 @@
 #include <linux/dma/qcom-gpi-dma.h>
 #include <linux/scatterlist.h>
 #include <linux/slab.h>
+#include <linux/soc/qcom/qcom_ccu_qup.h>
 #include "../dmaengine.h"
 #include "../virt-dma.h"
 
@@ -314,10 +315,6 @@ enum CNTXT_OFFS {
 	: (0x2005C + (0x4000 * (n)) + (0x80 * (k))))
 #define GPII_n_CH_k_QOS_PREFETCH_MODE		(0x400)
 
-/* GPII general control register */
-#define GPI_REE_CFG_OFFS			0x38
-#define GPI_RFE_INT_FAIR_RR_ARB_EN		BIT(16)
-
 /* Scratch registers */
 #define GPII_n_CH_k_SCRATCH_0_OFFS(n, k, ccu)\
 	((ccu) ? (0x20060 + (0x1000 * (n)) + (0x80 * (k)))\
@@ -694,7 +691,6 @@ struct gpii {
 	bool configured_irq;
 	enum gpi_pm_state pm_state;
 	rwlock_t pm_lock;
-	rwlock_t irq_lock;
 	struct gpi_ring ev_ring;
 	struct tasklet_struct ev_task; /* event processing tasklet */
 	struct completion cmd_completion;
@@ -823,8 +819,9 @@ void ccu_gpi_configure_mode(struct dma_chan *chan, int mode, int type)
 	gchan->ch_type = type;
 	if (mode) {
 		/* Enable the Global & IEOB IRQ of GPII in CDM mode */
-		gpi_update_reg(gpii, GPII_n_CNTXT_TYPE_COMP_IRQ_MSK_OFFS(gpii->gpii_id, gpii->gpi_dev->cfg.irq_off),
-			       GPII_n_CNTXT_TYPE_COMP_IRQ_MSK_BMSK, enable);
+		gpi_update_reg(gpii, GPII_n_CNTXT_TYPE_COMP_IRQ_MSK_OFFS(gpii->gpii_id,
+					   gpii->gpi_dev->cfg.irq_off),
+					   GPII_n_CNTXT_TYPE_COMP_IRQ_MSK_BMSK, enable);
 	}
 }
 EXPORT_SYMBOL(ccu_gpi_configure_mode);
@@ -1236,14 +1233,24 @@ static irqreturn_t gpi_handle_irq(int irq, void *data)
 }
 
 /* Handle IRQ for GPII */
-void ccu_gpi_handle_isr(void *dev, int idx)
+static void ccu_gpi_handle_isr(struct platform_device *pdev, int idx)
 {
-	struct gpi_dev *gpi = (struct gpi_dev *)dev;
-	struct gpii *gpii = &gpi->gpiis[idx];
+	struct gpi_dev *gpi;
+	struct gpii *gpii;
 
-	process_gpi_irq(gpii);
+	if (!pdev) {
+		pr_err("%s: pdev is NULL\n", __func__);
+		return;
+	}
+
+	gpi = platform_get_drvdata(pdev);
+	gpii = &gpi->gpiis[idx];
+
+	if (gpii->gchan[0].ch_ring.configured)
+		process_gpi_irq(gpii);
+	else
+		dev_err(gpii->gpi_dev->dev, "GPII[%d] is not configured\n", idx);
 }
-EXPORT_SYMBOL(ccu_gpi_handle_isr);
 
 /* process CCU Error notification event */
 static void gpi_process_ccu_error_event(struct gchan *gchan,
@@ -2541,7 +2548,8 @@ static void gpi_free_chan_resources(struct dma_chan *chan)
 	/* free all allocated memory */
 	gpi_free_ring(&gchan->ch_ring, gpii);
 	vchan_free_chan_resources(&gchan->vc);
-	kfree(gchan->config);
+	if (gchan->config)
+		kfree(gchan->config);
 
 	write_lock_irq(&gpii->pm_lock);
 	gchan->pm_state = DISABLE_STATE;
@@ -2743,8 +2751,10 @@ static int gpi_probe(struct platform_device *pdev)
 	if (!data)
 		return -EINVAL;
 
-	if (data->is_ccu_qup)
+	if (data->is_ccu_qup) {
 		gpi_dev->cfg = *data;
+		register_gpi_irq(ccu_gpi_handle_isr);
+	}
 
 	gpi_dev->ee_base = gpi_dev->ee_base - data->ee_offset;
 
@@ -2814,7 +2824,7 @@ static int gpi_probe(struct platform_device *pdev)
 			gchan->gpii = gpii;
 			gchan->dir = GPII_CHAN_DIR[chan];
 		}
-		/* */
+
 		mutex_init(&gpii->ctrl_lock);
 		rwlock_init(&gpii->pm_lock);
 
diff --git a/drivers/i2c/busses/i2c-qcom-geni.c b/drivers/i2c/busses/i2c-qcom-geni.c
index af43618b75ef..335dd94c2cb0 100644
--- a/drivers/i2c/busses/i2c-qcom-geni.c
+++ b/drivers/i2c/busses/i2c-qcom-geni.c
@@ -365,7 +365,6 @@ void geni_i2c_register_callback(ccu_qup_notify_client cb)
 {
 	ccu_qup_update_xfer_completion = cb;
 }
-EXPORT_SYMBOL(geni_i2c_register_callback);
 
 /*
  * geni_i2c_power_on_off - This function is used to turn on or off all the clock
@@ -376,7 +375,7 @@ EXPORT_SYMBOL(geni_i2c_register_callback);
  *
  * Return 0 on success or error on failure
  */
-int geni_i2c_power_on_off(struct i2c_adapter *adap, bool suspend)
+static int geni_i2c_power_on_off(struct i2c_adapter *adap, bool suspend)
 {
 	struct geni_i2c_dev *gi2c;
 	int ret, i;
@@ -424,7 +423,6 @@ int geni_i2c_power_on_off(struct i2c_adapter *adap, bool suspend)
 
 	return 0;
 }
-EXPORT_SYMBOL(geni_i2c_power_on_off);
 
 /*
  * geni_i2c_send_cmd_gsi_mode - This function is used to send bus cmd
@@ -613,7 +611,6 @@ int geni_i2c_check_recover_ios_line(struct i2c_adapter *adap)
 
 	return ret;
 }
-EXPORT_SYMBOL(geni_i2c_check_recover_ios_line);
 
 /*
  * geni_i2c_configure_ccu_mode : Configure the CCU mode and GPII channel type
@@ -625,10 +622,12 @@ EXPORT_SYMBOL(geni_i2c_check_recover_ios_line);
  *
  * Return 0 on success and error on failure.
  */
-int geni_i2c_configure_ccu_mode(struct i2c_adapter *adap, int gpii_no, int ch_type, bool mode)
+static int geni_i2c_configure_ccu_mode(struct i2c_adapter *adap, int gpii_no,
+				int ch_type, bool mode)
 {
 	struct geni_i2c_dev *gi2c;
 	int idx = (gpii_no % CCU_MAX_GPII);
+	int ret = 0;
 
 	if (!adap) {
 		pr_err("%s: Adap is NULL\n", __func__);
@@ -648,13 +647,22 @@ int geni_i2c_configure_ccu_mode(struct i2c_adapter *adap, int gpii_no, int ch_ty
 	ccu_gpi_configure_mode(gi2c->tx_c[idx], mode, ch_type);
 	ccu_gpi_configure_mode(gi2c->rx_c[idx], mode, ch_type);
 
-	release_gpi_dma(gi2c);
-	setup_gpi_dma(gi2c);
+	if (gi2c->rx_c[idx] && gi2c->tx_c[idx]) {
+		dma_release_channel(gi2c->rx_c[idx]);
+		gi2c->rx_c[idx] = NULL;
+		dma_release_channel(gi2c->tx_c[idx]);
+		gi2c->tx_c[idx] = NULL;
+	}
+
+	ret = setup_gpi_dma(gi2c);
+	if (ret) {
+		dev_err(gi2c->se.dev, "AR: Setup gpi dma failed\n");
+		return ret;
+	}
 	gi2c->ccu.gpi[idx].gpii_mode_change = false;
 
 	return 0;
 }
-EXPORT_SYMBOL(geni_i2c_configure_ccu_mode);
 
 /*
  * geni_i2c_gpii_reset : Reset the GPII channel.
@@ -665,7 +673,7 @@ EXPORT_SYMBOL(geni_i2c_configure_ccu_mode);
  *
  * Return 0 on success and error on failure.
  */
-int geni_i2c_gpii_reset(struct i2c_adapter *adap, int gpii_no,
+static int geni_i2c_gpii_reset(struct i2c_adapter *adap, int gpii_no,
 			bool premature_cancel)
 {
 	struct geni_i2c_dev *gi2c;
@@ -706,7 +714,6 @@ int geni_i2c_gpii_reset(struct i2c_adapter *adap, int gpii_no,
 
 	return 0;
 }
-EXPORT_SYMBOL(geni_i2c_gpii_reset);
 
 /*
  * geni_i2c_configure_delay : Configure inter transfer delay.
@@ -717,7 +724,7 @@ EXPORT_SYMBOL(geni_i2c_gpii_reset);
  *
  * Return none.
  */
-void geni_i2c_configure_pre_delay(struct i2c_adapter *adap, unsigned long time)
+static void geni_i2c_configure_pre_delay(struct i2c_adapter *adap, unsigned long time)
 {
 	struct geni_i2c_dev *gi2c;
 
@@ -734,7 +741,6 @@ void geni_i2c_configure_pre_delay(struct i2c_adapter *adap, unsigned long time)
 
 	gi2c->ccu.inter_delay = time;
 }
-EXPORT_SYMBOL(geni_i2c_configure_pre_delay);
 
 static void geni_i2c_err_misc(struct geni_i2c_dev *gi2c)
 {
@@ -781,7 +787,7 @@ static void geni_i2c_err(struct geni_i2c_dev *gi2c, int err)
 	}
 }
 
-static irqreturn_t geni_i2c_irq(int irq, void *dev)
+static void process_i2c_irq(int irq, void *dev)
 {
 	struct geni_i2c_dev *gi2c = dev;
 	void __iomem *base = gi2c->se.base;
@@ -877,10 +883,32 @@ static irqreturn_t geni_i2c_irq(int irq, void *dev)
 		complete(&gi2c->done);
 
 	spin_unlock(&gi2c->lock);
+}
 
+static irqreturn_t geni_i2c_irq(int irq, void *dev)
+{
+	process_i2c_irq(irq, dev);
 	return IRQ_HANDLED;
 }
 
+void geni_i2c_process_irq(struct i2c_adapter *adap)
+{
+	struct geni_i2c_dev *gi2c;
+
+	if (!adap) {
+		pr_err("%s: Adap is NULL\n", __func__);
+		return;
+	} else {
+		gi2c = adap->dev.driver_data;
+		if (!gi2c) {
+			pr_err("%s: I2C driver data is NULL\n", __func__);
+			return;
+		}
+	}
+	process_i2c_irq(gi2c->irq, gi2c);
+}
+
+
 static void geni_i2c_abort_xfer(struct geni_i2c_dev *gi2c)
 {
 	unsigned long time_left = ABORT_TIMEOUT;
@@ -1095,7 +1123,9 @@ static void i2c_gpi_cb_result(void *cb, const struct dmaengine_result *result)
 	}
 
 	gpii_idx = result->gpii_id % CCU_MAX_GPII;
-	frame_idx = gi2c->ccu.gpi[gpii_idx].released_cnt;
+	if (gi2c->is_ccu_qup)
+		frame_idx = gi2c->ccu.gpi[gpii_idx].released_cnt;
+
 	if (gi2c->is_ccu_qup && gi2c->ccu.gpi[gpii_idx].frame[frame_idx].async) {
 		gi2c->ccu.result.gpii_no = result->gpii_id;
 		gi2c->ccu.result.adap_nr = gi2c->adap.nr;
@@ -1126,7 +1156,8 @@ static void geni_i2c_gpi_unmap(struct geni_i2c_dev *gi2c, struct i2c_msg *msg,
 
 	if (rx_buf) {
 		dma_unmap_single(gi2c->se.dev->parent, rx_addr, len, DMA_FROM_DEVICE);
-		i2c_put_dma_safe_msg_buf(rx_buf, msg, !gi2c->err);
+		if (!gi2c->is_ccu_qup)
+			i2c_put_dma_safe_msg_buf(rx_buf, msg, !gi2c->err);
 	}
 }
 
@@ -1178,20 +1209,39 @@ static int geni_i2c_gpi(struct geni_i2c_dev *gi2c, struct i2c_msg *msg,
 
 	if (op == I2C_WRITE && gi2c->is_ccu_qup) {
 		/* set the TX sub pkt_len */
-		peripheral_cfg->rx_len = gi2c->ccu.msg[idx]->pkt_len;
-		peripheral_cfg->ccu_cfg.rand_wr = true;
+		if (gi2c->ccu.msg[idx]->pkt_len > 0) {
+			peripheral_cfg->ccu_cfg.rand_wr = false;
+			peripheral_cfg->rx_len = gi2c->ccu.msg[idx]->pkt_len;
+		} else {
+			peripheral_cfg->rx_len = len;
+		}
 	} else {
 		/* set the length as message for rx txn */
 		peripheral_cfg->rx_len = len;
 	}
 	peripheral_cfg->op = op;
-	peripheral_cfg->delay = gi2c->ccu.inter_delay;
-	peripheral_cfg->ccu_cfg.ccu_last = gi2c->ccu.msg[idx]->ccu_last;
-	peripheral_cfg->ccu_cfg.ch_last = gi2c->ccu.msg[idx]->ch_last;
-	peripheral_cfg->ccu_cfg.nop_frame = gi2c->ccu.msg[idx]->nop_frame;
-	peripheral_cfg->post_delay = gi2c->ccu.msg[idx]->post_delay;
-	peripheral_cfg->pre_delay = gi2c->ccu.msg[idx]->pre_delay;
-	peripheral_cfg->ccu_cfg.retry = gi2c->ccu.msg[idx]->arb_lost_retry;
+
+	if (gi2c->is_ccu_qup) {
+		if ((gi2c->msgs == 1) && (msg->flags & I2C_M_RD) && (op == I2C_WRITE))
+			peripheral_cfg->ccu_cfg.ccu_last = 0;
+		else
+			peripheral_cfg->ccu_cfg.ccu_last = gi2c->ccu.msg[idx]->ccu_last;
+
+		peripheral_cfg->ccu_cfg.ch_last = gi2c->ccu.msg[idx]->ch_last;
+		peripheral_cfg->ccu_cfg.nop_frame = gi2c->ccu.msg[idx]->nop_frame;
+		peripheral_cfg->post_delay = gi2c->ccu.msg[idx]->post_delay;
+		peripheral_cfg->pre_delay = gi2c->ccu.msg[idx]->pre_delay;
+		peripheral_cfg->ccu_cfg.retry = gi2c->ccu.msg[idx]->arb_lost_retry;
+		peripheral_cfg->delay = gi2c->ccu.inter_delay;
+	} else {
+		peripheral_cfg->ccu_cfg.ccu_last = 0;
+		peripheral_cfg->ccu_cfg.ch_last = 0;
+		peripheral_cfg->ccu_cfg.nop_frame = 0;
+		peripheral_cfg->post_delay = 0;
+		peripheral_cfg->pre_delay = 0;
+		peripheral_cfg->ccu_cfg.retry = 0;
+		peripheral_cfg->delay = 0;
+	}
 
 	ret = dmaengine_slave_config(dma_chan, config);
 	if (ret) {
@@ -1360,6 +1410,7 @@ static int geni_i2c_gpi_xfer(struct geni_i2c_dev *gi2c, struct i2c_msg msgs[], i
 		      __func__, num, msgs[0].addr);
 	for (i = 0; i < num; i++) {
 		gi2c->cur = &msgs[i];
+		gi2c->msgs = num;
 		gi2c->err = 0;
 		i2c_trace_log(gi2c->se.dev, "msg[%d].len:%d\n", i, gi2c->cur->len);
 
@@ -1474,14 +1525,9 @@ static int geni_i2c_gpi_xfer(struct geni_i2c_dev *gi2c, struct i2c_msg msgs[], i
 			kfree(gi2c->ccu.msg[k]);
 			gi2c->ccu.msg[k] = NULL;
 		}
-		for (k = 0; k < CCU_MAX_GPII; k++) {
-			dmaengine_terminate_sync(gi2c->rx_c[k]);
-			dmaengine_terminate_sync(gi2c->tx_c[k]);
-		}
-	} else {
-		dmaengine_terminate_sync(gi2c->rx_c[idx]);
-		dmaengine_terminate_sync(gi2c->tx_c[idx]);
 	}
+	dmaengine_terminate_sync(gi2c->rx_c[idx]);
+	dmaengine_terminate_sync(gi2c->tx_c[idx]);
 	geni_i2c_gpi_unmap(gi2c, &msgs[i], tx_buf, tx_addr, rx_buf, rx_addr);
 	return ret;
 }
@@ -1590,6 +1636,16 @@ static void release_gpi_dma(struct geni_i2c_dev *gi2c)
 	}
 }
 
+static inline void init_tx_rx_ch(struct geni_i2c_dev *gi2c)
+{
+	int i = 0;
+
+	for (i = 0; i < CCU_MAX_GPII; i++) {
+		gi2c->tx_c[i] = NULL;
+		gi2c->rx_c[i] = NULL;
+	}
+}
+
 static int setup_gpi_dma(struct geni_i2c_dev *gi2c)
 {
 	int ret;
@@ -1608,22 +1664,26 @@ static int setup_gpi_dma(struct geni_i2c_dev *gi2c)
 
 	geni_se_select_mode(&gi2c->se, GENI_GPI_DMA);
 	do {
-		i2c_trace_log(gi2c->se.dev, "%s:[%d] Request TX chan:%s\n", __func__, i, tx_ch[i]);
-		gi2c->tx_c[i] = dma_request_chan(gi2c->se.dev, tx_ch[i]);
-		if (IS_ERR(gi2c->tx_c[i])) {
-			ret = dev_err_probe(gi2c->se.dev, PTR_ERR(gi2c->tx_c[i]),
-					    "Failed to get tx%d DMA ch\n", i);
-			goto err;
+		if (!gi2c->tx_c[i]) {
+			i2c_trace_log(gi2c->se.dev, "%s:[%d] Request TX chan:%s\n", __func__, i, tx_ch[i]);
+			gi2c->tx_c[i] = dma_request_chan(gi2c->se.dev, tx_ch[i]);
+			if (IS_ERR(gi2c->tx_c[i])) {
+				ret = dev_err_probe(gi2c->se.dev, PTR_ERR(gi2c->tx_c[i]),
+							"Failed to get tx%d DMA ch\n", i);
+				goto err;
+			}
 		}
 
-		i2c_trace_log(gi2c->se.dev, "%s:[%d] Request RX chan:%s\n", __func__, i, rx_ch[i]);
-		gi2c->rx_c[i] = dma_request_chan(gi2c->se.dev, rx_ch[i]);
-		if (IS_ERR(gi2c->rx_c[i])) {
-			ret = dev_err_probe(gi2c->se.dev, PTR_ERR(gi2c->rx_c[i]),
-					    "Failed to get rx%d DMA ch\n", i);
-			dma_release_channel(gi2c->tx_c[i]);
-			gi2c->tx_c[i] = NULL;
-			goto err;
+		if (!gi2c->rx_c[i]) {
+			i2c_trace_log(gi2c->se.dev, "%s:[%d] Request RX chan:%s\n", __func__, i, rx_ch[i]);
+			gi2c->rx_c[i] = dma_request_chan(gi2c->se.dev, rx_ch[i]);
+			if (IS_ERR(gi2c->rx_c[i])) {
+				ret = dev_err_probe(gi2c->se.dev, PTR_ERR(gi2c->rx_c[i]),
+							"Failed to get rx%d DMA ch\n", i);
+				dma_release_channel(gi2c->tx_c[i]);
+				gi2c->tx_c[i] = NULL;
+				goto err;
+			}
 		}
 	} while (++i < idx);
 
@@ -1748,6 +1808,7 @@ static int geni_i2c_init(struct geni_i2c_dev *gi2c)
 		fifo_disable = readl_relaxed(gi2c->se.base + GENI_IF_DISABLE_RO) & FIFO_IF_DISABLE;
 
 	if (fifo_disable) {
+		init_tx_rx_ch(gi2c);
 		/* FIFO is disabled, so we can only use GPI DMA */
 		ret = setup_gpi_dma(gi2c);
 		if (ret) {
@@ -1873,6 +1934,15 @@ static int geni_i2c_probe(struct platform_device *pdev)
 	}
 
 	gi2c->adap.algo = &geni_i2c_algo;
+
+	if (gi2c->is_ccu_qup) {
+		register_i2c_function(geni_i2c_power_on_off,
+				geni_i2c_check_recover_ios_line,
+				geni_i2c_configure_ccu_mode,
+				geni_i2c_gpii_reset,
+				geni_i2c_configure_pre_delay,
+				geni_i2c_process_irq);
+	}
 	init_completion(&gi2c->done);
 	spin_lock_init(&gi2c->lock);
 	platform_set_drvdata(pdev, gi2c);
diff --git a/drivers/soc/qcom/qcom_ccu_qup.c b/drivers/soc/qcom/qcom_ccu_qup.c
index f02773d047f0..fec39eccb659 100644
--- a/drivers/soc/qcom/qcom_ccu_qup.c
+++ b/drivers/soc/qcom/qcom_ccu_qup.c
@@ -34,6 +34,35 @@
 
 struct ccu_qup *ccud;
 
+static power_on_off_t ccu_on_off;
+static recover_ios_line_t ccu_recover;
+static config_ccu_mode_t ccu_cfg_mode;
+static gpii_reset_t ccu_reset;
+static config_delay_t ccu_delay;
+static ccu_gpi_handle_isr_t gpi_handle_irq;
+static geni_i2c_irq_t i2c_handle_irq;
+void register_i2c_function(power_on_off_t geni_i2c_power_on_off,
+		recover_ios_line_t geni_i2c_check_recover_ios_line,
+		config_ccu_mode_t geni_i2c_configure_ccu_mode,
+		gpii_reset_t geni_i2c_gpii_reset,
+		config_delay_t geni_i2c_configure_pre_delay,
+		geni_i2c_irq_t geni_i2c_process_irq)
+{
+	ccu_on_off = geni_i2c_power_on_off;
+	ccu_recover = geni_i2c_check_recover_ios_line;
+	ccu_cfg_mode = geni_i2c_configure_ccu_mode;
+	ccu_reset = geni_i2c_gpii_reset;
+	ccu_delay = geni_i2c_configure_pre_delay;
+	i2c_handle_irq = geni_i2c_process_irq;
+}
+EXPORT_SYMBOL(register_i2c_function);
+
+void register_gpi_irq(ccu_gpi_handle_isr_t ccu_gpi_handle_isr)
+{
+	gpi_handle_irq = ccu_gpi_handle_isr;
+}
+EXPORT_SYMBOL(register_gpi_irq);
+
 static const struct file_operations ccu_fops = {
 	.owner = THIS_MODULE,
 	.write = ccu_config_write,
@@ -162,7 +191,6 @@ static int ccu_release(struct inode *inode, struct file *filp)
 		return -EINVAL;
 
 	ccu = filp->private_data;
-	geni_i2c_register_callback(ccu_update_xfer_result);
 
 	return 0;
 }
@@ -191,7 +219,7 @@ int ccu_power_on_off(struct i2c_adapter *adap, bool suspend)
 	if (!adap)
 		return -EINVAL;
 
-	ret = geni_i2c_power_on_off(adap, suspend);
+	ret = ccu_on_off(adap, suspend);
 	if (ret) {
 		pr_err("failed to turn on/off the clocks:%d\n", suspend);
 		return -EAGAIN;
@@ -205,7 +233,7 @@ void ccu_check_recover_bus(struct i2c_adapter *adap)
 	if (!adap)
 		return;
 
-	geni_i2c_check_recover_ios_line(adap);
+	ccu_recover(adap);
 }
 
 /*
@@ -221,7 +249,7 @@ void ccu_configure_inter_trans_delay(struct i2c_adapter *adap, unsigned long tim
 	if (!adap)
 		return;
 
-	geni_i2c_configure_pre_delay(adap, time);
+	ccu_delay(adap, time);
 }
 
 static void ccu_update_ch_id_mapping(struct ccu_qup *ccu, int qup_idx, int id,
@@ -341,7 +369,7 @@ int ccu_configure_mode(struct i2c_adapter *adap, struct ccu_qup_config *cfg)
 	if (!adap || !cfg)
 		return -EINVAL;
 
-	ret = geni_i2c_configure_ccu_mode(adap, cfg->gpii_no,
+	ret = ccu_cfg_mode(adap, cfg->gpii_no,
 					  cfg->ch_type, cfg->ccu_mode);
 	if (ret) {
 		pr_err("%s: Configuration of CCU Mode is failed for gpii-%d of i2c-%d\n",
@@ -391,12 +419,12 @@ int ccu_gpii_reset(struct i2c_adapter *adap, int gpii_no, bool partial,
 		return -EINVAL;
 
 	if (partial) {
-		ret = geni_i2c_gpii_reset(adap, gpii_no, premature_cancel);
+		ret = ccu_reset(adap, gpii_no, premature_cancel);
 		if (ret)
 			return ret;
 	} else {
 		for (i = 0; i < CCU_MAX_GPII_PER_SE; i++) {
-			ret = geni_i2c_gpii_reset(adap, i, premature_cancel);
+			ret = ccu_reset(adap, i, premature_cancel);
 			if (ret)
 				return ret;
 		}
@@ -526,20 +554,31 @@ static ssize_t ccu_config_write(struct file *filp, const char __user *buf,
 static irqreturn_t gpi0_irq_thread(int irq, void *dev_id)
 {
 	struct ccu_qup *ccu = (struct ccu_qup *)dev_id;
-	u32 irq_stat, i;
+	u32 irq_stat = ccu->ccu0_irq_stat;
+	u32 i;
 	unsigned long flag;
 
 	spin_lock_irqsave(&ccu->lock0, flag);
+
 	irq_stat = readl_relaxed(ccu->qup_irq_base[0] + CCU_QUP_IRQ_STATUS);
-	/* GPII index starts from 5th position */
-	for (i = GPII_0_IRQ; i < GPII_14_IRQ; i++) {
-		if (irq_stat & (1U << i)) {
-			ccu_gpi_handle_isr(ccu->gpi_dev[0], i);
+
+	/* Clear the interrupt */
+	writel(irq_stat, (ccu->qup_irq_base[0] + CCU_QUP_IRQ_CLEAR));
+	writel(0x1, (ccu->qup_irq_base[0] + CCU_QUP_IRQ_CMD));
+
+	/* Handle the IRQ line, GPII index starts from 5th position */
+	for (i = GPII_0_IRQ; i <= GPII_14_IRQ; i++) {
+		if (i >= 0 && i < 5) {
+			if (irq_stat & (1U << i)) {
+				i2c_handle_irq(ccu->adap);
+			}
+		} else {
+			if (irq_stat & (1U << i)) {
+				gpi_handle_irq(ccu->gpi_pdev[0], (i - 5));
+			}
 		}
 	}
 
-	/* Clear the interrupt */
-	writel_relaxed(i, (ccu->qup_irq_base[0] + CCU_QUP_IRQ_CLEAR));
 	spin_unlock_irqrestore(&ccu->lock0, flag);
 
 	return IRQ_HANDLED;
@@ -553,20 +592,31 @@ static irqreturn_t ccu0_irq_handler(int irq, void *dev_id)
 static irqreturn_t gpi1_irq_thread(int irq, void *dev_id)
 {
 	struct ccu_qup *ccu = (struct ccu_qup *)dev_id;
-	u32 irq_stat, i;
+	u32 irq_stat = ccu->ccu1_irq_stat;
+	u32 i;
 	unsigned long flag;
 
 	spin_lock_irqsave(&ccu->lock1, flag);
+
 	irq_stat = readl_relaxed(ccu->qup_irq_base[1] + CCU_QUP_IRQ_STATUS);
-	/* GPII index starts from 5th position */
-	for (i = GPII_0_IRQ; i < GPII_14_IRQ; i++) {
-		if (irq_stat & (1U << i)) {
-			ccu_gpi_handle_isr(ccu->gpi_dev[1], i);
+
+	/* Clear the interrupt */
+	writel(irq_stat, (ccu->qup_irq_base[1] + CCU_QUP_IRQ_CLEAR));
+	writel(0x1, (ccu->qup_irq_base[1] + CCU_QUP_IRQ_CMD));
+
+	/* Handle the IRQ line, GPII index starts from 5th position */
+	for (i = GPII_0_IRQ; i <= GPII_14_IRQ; i++) {
+		if (i >= 0 && i < 5) {
+			if (irq_stat & (1U << i)) {
+				i2c_handle_irq(ccu->adap);
+			}
+		} else {
+			if (irq_stat & (1U << i)) {
+				gpi_handle_irq(ccu->gpi_pdev[1], (i - 5));
+			}
 		}
 	}
 
-	/* Clear the interrupt */
-	writel_relaxed(irq_stat, (ccu->qup_irq_base[1] + CCU_QUP_IRQ_CLEAR));
 	spin_unlock_irqrestore(&ccu->lock1, flag);
 
 	return IRQ_HANDLED;
@@ -626,7 +676,7 @@ static void get_set_child_dev_node(struct work_struct *work)
 	if (!child_status->probe_state) {
 		dev_err(ccu->dev, "CCU child device are not yet probed\n");
 		queue_delayed_work(ccu->gpi_wq, &ccu->get_child_dev_node,
-				   msecs_to_jiffies(3000));
+				   msecs_to_jiffies(3));
 		kfree(child_status);
 		return;
 	}
@@ -837,9 +887,9 @@ static int ccu_qup_probe(struct platform_device *pdev)
 	}
 
 	ccud = ccu;
-	/* Queue the delayed work to be executed after 5 seconds */
+	/* Queue the delayed work to be executed after 3 seconds */
 	queue_delayed_work(ccu->gpi_wq, &ccu->get_child_dev_node,
-			   msecs_to_jiffies(5000));
+			   msecs_to_jiffies(3000));
 
 	dev_dbg(&pdev->dev, "CCU QUP Probe completed\n");
 
diff --git a/include/linux/dma/qcom-gpi-dma.h b/include/linux/dma/qcom-gpi-dma.h
index e779339a7dae..6527c98b1510 100644
--- a/include/linux/dma/qcom-gpi-dma.h
+++ b/include/linux/dma/qcom-gpi-dma.h
@@ -127,6 +127,4 @@ void ccu_gpi_configure_mode(struct dma_chan *chan, int mode, int type);
 
 int ccu_gpi_prioritize_chan(struct dma_chan *chan);
 
-void ccu_gpi_handle_isr(void *dev, int idx);
-
 #endif /* QCOM_GPI_DMA_H */
diff --git a/include/linux/soc/qcom/qcom_ccu_qup.h b/include/linux/soc/qcom/qcom_ccu_qup.h
index 168880d57e29..a1732d7cc17f 100644
--- a/include/linux/soc/qcom/qcom_ccu_qup.h
+++ b/include/linux/soc/qcom/qcom_ccu_qup.h
@@ -169,18 +169,28 @@ int ccu_qup_load_firmware(void);
 
 void ccu_configure_inter_trans_delay(struct i2c_adapter *adap, unsigned long time);
 
-int geni_i2c_configure_ccu_mode(struct i2c_adapter *adap, int gpii_no, int ch_type,
-				bool mode);
+void ccu_update_xfer_result(struct ccu_xfer_result *result);
 
-int geni_i2c_gpii_reset(struct i2c_adapter *adap, int gpii_no, bool premature_cancel);
+typedef int (*power_on_off_t) (struct i2c_adapter *adap, bool suspend);
 
-void geni_i2c_configure_pre_delay(struct i2c_adapter *adap, unsigned long time);
+typedef int (*recover_ios_line_t) (struct i2c_adapter *adap);
 
-int geni_i2c_check_recover_ios_line(struct i2c_adapter *adap);
+typedef int (*config_ccu_mode_t) (struct i2c_adapter *adap, int gpii_no, int ch_type, bool mode);
 
-int geni_i2c_power_on_off(struct i2c_adapter *adap, bool suspend);
+typedef int (*gpii_reset_t) (struct i2c_adapter *adap, int gpii_no, bool premature_cancel);
 
-void ccu_update_xfer_result(struct ccu_xfer_result *result);
+typedef void (*config_delay_t) (struct i2c_adapter *adap, unsigned long delay);
+
+typedef void (*geni_i2c_irq_t) (struct i2c_adapter *adap);
+
+void register_i2c_function(power_on_off_t geni_i2c_power_on_off,
+		recover_ios_line_t geni_i2c_check_recover_ios_line,
+		config_ccu_mode_t geni_i2c_configure_ccu_mode,
+		gpii_reset_t geni_i2c_gpii_reset,
+		config_delay_t geni_i2c_configure_pre_delay,
+		geni_i2c_irq_t geni_i2c_process_irq);
+
+typedef void (*ccu_gpi_handle_isr_t) (struct platform_device *dev, int idx);
 
-void geni_i2c_register_callback(ccu_qup_notify_client cb);
+void register_gpi_irq(ccu_gpi_handle_isr_t);
 #endif /* _QCOM_CCU_QUP_H_ */
-- 
2.17.1

